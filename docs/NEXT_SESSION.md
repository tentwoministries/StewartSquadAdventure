## Working Rules (apply to every session, every agent, every file)

These are Andrew's standing rules. They go at the top of every handoff document, every subagent system prompt, and `CLAUDE.md`. Subagents do not inherit the parent session's context, so each agent file must carry these verbatim.

1. **Missing inputs → stop and ask.** If a file, data source, or piece of context needed to do a task *properly* is missing, stop and ask Andrew rather than shipping incomplete work wrapped in disclaimers. Never paper over gaps. (Creative decisions are *not* missing inputs — see docs/BRIEF.md §2. Decide, log, proceed.)
2. **Pre-build gate for every external interface.** Before writing integration code against any API, package, or data format (Rapier, postprocessing, Colyseus, GLB loaders, save schema, Puppeteer GL flags), hit it with a real call and verify the actual response/shape. Never code against assumed or documented field names. Record the verified shape in the task's notes before proceeding. Non-negotiable.
3. **No assumptions in code.** Verify every field name, handler name, parameter, and format against the real source before writing tests, configs, or integration code. If unsure, grep the codebase first.
4. **Single Pro Inspection Checklist V2 — all 27 steps, every delivery, no tiers.** (Full list: docs/INSPECTION_CHECKLIST.md)
   - P0 pre-build: memory/handoff read, design approval on record, API verification done, inherited code audited
   - P1 static: `tsc --noEmit` clean, runtime import check, AST/import trace, duplicate definitions, TODO sweep
   - P2 semantic: call-chain trace, entry points, name collisions, fuzzy/near-duplicate logic, substring hazards, possessive/string hazards in content, router/state-machine transitions
   - P3 infra: priority ordering, data field coverage, async correctness, rate/frame budgets, deferred work listed, exclusions documented
   - P4 delivery: filenames, packaging, regression suite green, version stamp, line/size count
5. **Handoff is the source of truth.** Every session ends by writing `docs/NEXT_SESSION.md` with these Working Rules at the top, current state, what's done, what's next, and known issues. The next session starts by reading it.
6. **Deliverables are files, not chat.** Everything lands in the repo. Andrew never copy-pastes from chat.
7. **Tests: trim before adding.** Before generating new tests, do a trim pass on the existing suite; target ~100 tests per suite; propose specific trims with rationale before writing new ones.

**Family canon is sacred.** The kids' names, core personalities, and canon text never change: **Liam** (oldest, protective, steady — the leader), **Noah** (sharp, quick, independent), **Collette** (creative, imaginative), **Isabella** (youngest, fierce, unstoppable). Grandpa Ed flies the biplane. Each kid's color and role are set in the Design Bible (`docs/design/heroes.md`, Phase 0.5) and may be revised only there, with the rationale logged in `docs/DECISIONS.md`; Brief §1 is the starting point, not the law. Existing dialogue, quest text, in-jokes, cutscene lines, voice lines, achievement names, and loading tips are ported **verbatim** — never paraphrased or "improved". New text may be added. Nothing ships that isn't family-friendly.

## Routing (2026-09-08, after the reel fixes round 1): read docs/LANES.md first

This checkout is lane **0.75 visual studies** (the default lane, the demo lane) on branch `phase-0.75-visual-studies`. If Andrew's first line names another lane, move to that lane's worktree before reading further. Open lanes: 0.75 visual studies (this checkout, :5173), 0.75 demo (`StewartSquad-demo/`, now at tag `p0.75-demo-reel-2`, :5180), 0.75 scratch (`StewartSquad-scratch/`, branch `phase-0.75-scratch`, re-cut at this lane's tip, :5181), 0.85 story and play (`StewartSquad-story/`, branch `phase-0.85-story`; it received the routing docs 2026-09-08), and, once its session cuts it, the Fable control (`StewartSquad-control/`, branch `phase-0.75-fable-control`, :5183). The Opus experiment lane is **closed** (branch `phase-0.75-opus-experiment` kept as the record at `59da9a6`; its worktree and folder are gone). Default when unnamed: this lane. Opening phrases: "demo candidates" → this lane, `docs/design/mockups/DEMO_PROGRAM.md` §2; "control build" / "control review" → the control lane, `FABLE_CONTROL_BRIEF.md`; "show the kids" / "open the demo" → the demo worktree on :5180.

## Model and effort policy (Andrew, 2026-09-06; the delegated loop 2026-09-08)

- Orchestrator: Fable 5.1 at **high**, every session. At session start, confirm `CLAUDE_CODE_EFFORT_LEVEL` is unset (verified unset 2026-09-08).
- `art-director`, `qa-inspector`, `design-lead`: `fable` / `xhigh`; the `-max` variants only for Phase 1 excellence-mark scoring and phase-gate reviews.
- Implementers and `sandbox-builder`: `opus` / `high`. `rules-librarian`, `rules-auditor`, `archaeologist` and mechanical work: `opus` / `medium`.
- **The delegated loop is now proven on a full session** (`DEMO_PROGRAM.md` §6; this session was its pilot): Fable plans, writes the briefs, reviews reports and frames, judges, commits. Every Opus prompt is self-contained (brief file + rule sheet file + the checks + the harness); a wrong result goes to a fresh Opus fixer twice, then to Fable. Fable wrote no scene code this session. The sandbox-direct exception for the orchestrator is retired for anything an agent can be briefed on.

---

# Next session — handoff written 2026-09-08 (end of **the reel fixes, round 1**; Fable orchestrated by Andrew's call, seven Opus builders, one auditor, one fixer; nine commits from `9c86a18` to the close commit; **Andrew has not yet looked at round 1** — that is the next thing)

## Systems at a glance (the one-page map is `docs/design/mockups/README.md`)

| Piece | Where | State |
|---|---|---|
| Design Bible (not edited in 0.75) | `docs/design/*.md` | `p0.5-design-bible` on `main` |
| **The demo reel: nine scenes** | `sandbox/index.html` (tabs Places / Beats / Systems / Lab) · Places `forest-dusk/ desert-noon/ bog-night/ frozen-night/ caves-descent/ rim-dawn/` · Beats `shadow-wrong/ meadow-golden/` (+ the caves) · Lab `flight-golden/` | **round 1 built on this branch, not yet tagged**; the demo worktree (:5180) still sits on `p0.75-demo-reel-2` |
| The shared runtime | `sandbox/_shared/scene.ts` (+ `look`, `flourishOk`, hit-stop), `walk.ts` (sprint lock), `rig.ts` (curved ring at +0.04, `setGround`, IK helpers, `ssRigProbe`), `kid-collette.ts` (the staff solved into the hand), `kid-isabella.ts` (the hammer's four carries, the curved ribbon), **`rock.ts`** (per-position displacement, `openEdges`), `step.ts`, `shot.ts` | landed this round; nine frames byte-identical across the last two sweeps |
| The headless harness | `scripts/sandbox-drive.cjs` + `scripts/probes/*.cjs` (**~57 probes**: runtime-*, shadow-*, meadow-*, rim-*, flight-*, shared-*, rocks-*, bog-*, frozen-*, caves-*, `runtime-stations.cjs` (59 stations), `round1-frames.cjs` (one frame per scene)) | every agent drives a page with it; the dev server must be up first (`preview_start` `vite-dev`, :5173) |
| Tests | `tests/unit/sandbox/{flight 9, place 11, goblins 4, rock 8, caves 6}.test.ts`, `tests/unit/version.test.ts` | **39 pass**; `npm run check` green at the close |
| The ledger | `docs/design/mockups/LESSONS.md` | §0 (12 lines), Camera, Rigs (+5 this round), Light, Density (+2), **Geometry** (new, T-43), Process, Misc (+2) |
| Rules per task | `docs/qa/briefs/reel-fixes-*-03.md` (7) · `docs/qa/rules/reel-fixes-*-03.md` (7) · **`docs/qa/rules/reel-fixes-03-audit.md`** (10 items, all acted on; §3 lists every deferral) | the pattern for every round |
| Tweak rows | `docs/design/PHASE_0.75_TWEAKS.md` **T-01..T-60** | T-41..T-56 this round's reactions (13 `built`, T-44/T-48 `proposed; built`, T-46 `story-dependent`); T-57 the rigs' handedness (`observed`); **T-58..T-60 round-2 intake from Andrew's screenshots** (T-60 fixed) |
| The session record | `docs/design/mockups/REEL_FIXES_BRIEF.md` **§5** (the run table, the cost, the call for future rounds), `docs/PROGRESS.md` (the round's entry), `docs/design/mockups/LOG.md` (nine new sections) | |
| Frames | `docs/design/mockups/*-03-*.png` (~110 this round) — the delivered ones are named in each LOG section; **`<scene>-round1-03-02.png`** ×9 is the merged tree | committed |
| Branches / worktrees | `phase-0.75-visual-studies` (this; nine commits `9c86a18`…close) · `phase-0.75-scratch` (behind, at `ca5f90e`) · `phase-0.85-story` · `phase-0.75-opus-experiment` (closed) · `main` untouched | |

## Where we are

- **Round 1 of the reel fixes is built, audited, fixed and committed; Andrew has not seen it.** Sixteen of his reactions (rows T-41..T-56) became seven briefs; every fix carries a probe's numbers in `LOG.md`. Headlines: the selection ring and the whirl ribbon bend with the curved world (0.8 px at 30 m; was 53 px up the body); Collette's staff is in her hand; Isabella's hammer rests on the ground at idle, drags at walk and rides the shoulder at run; ⇧⇧ sprints at 5.4 m/s; every displaced rock is a closed surface (`_shared/rock.ts`); Home Wrong S4 walks; the Bog's frogs live on real pads and Fern is a person facing the causeway; the Hearth's drift runs downhill to the lake with penguins belly-down, seals roll about their own axis and hump-crawl, the pool lies in a dish and reads as ice, the herd crosses at 20 s and 90 s unprompted; the caves' stair is cut into the tiers with a flared mouth and flat column tops, four whole rings on the landing; the flight has an optional look that eases home; and X no longer fires a whirl in the cockpit (T-60, from Andrew's screenshot the same evening).
- **Two things Andrew must judge by looking** rather than a probe: (a) **the Bog's S2 and W1** — he said Collette could not walk there; the probe walks her 7.8 m in four seconds at S2 and 4.5 m at W1, and the placements were left alone (`runtime-stations.cjs` is the reproduction tool: if it happens again, note the exact keys pressed); (b) whether Fern, the pool's ice and the caves' stair read at his screen.
- **The meta-question he asked** (does delegating to Opus add work for Fable?) is answered in `REEL_FIXES_BRIEF.md` §5 with the run table: ~3 M Opus tokens over 71 min of parallel build for what would have been 1.5–2 M Fable tokens and five to seven hours serial; the seams (a ring lift both briefs assigned to the other; no frame of the merged tree until the fixer) are the cost, and two standing steps were added to §2 (name one owner when two briefs touch one object; end every round with a merged-tree sweep). The call: keep the shape for rounds of three or more scenes or any `_shared/` change; the Opus-run default for small scene-local rounds; Fable edits code only when the brief would be longer than the diff.
- **Toolchain:** `npm run check` green (tsc, eslint, 39 tests); `vite.config.ts` now ignores `docs/**` (Andrew's screenshots in `docs/reference/Screenshots/` crashed the watcher twice); `CLAUDE_CODE_EFFORT_LEVEL` verified unset.
- **Design Bible:** unchanged (Phase 0.75 rule 1). Phase 0 part B still blocked on the brainstorm doc (Rule 1); the three legacy inputs still missing from `docs/legacy/`.

## What to do next session (in order)

1. Read `docs/LANES.md`, this file, `CLAUDE.md`, `REEL_FIXES_BRIEF.md` (§2 and §5), `LESSONS.md` §0, and the round's LOG sections for anything Andrew names. Confirm `CLAUDE_CODE_EFFORT_LEVEL` is unset. Start the dev server with `preview_start` `vite-dev`.
2. **Andrew's look at round 1** on :5173 (the hub → Beats tab first: Home Wrong S4, Crash Meadow; then the Hearth (PG, S2 at 30 s, S4), the Bog (S1, S2, W1, the jetty), the caves (S1, S2, the stair), the flight (drag during the ride, X does nothing)). Yes/no per item; a "no" goes to a fresh builder with what he saw. When it reads: tag **`p0.75-demo-reel-3`**, move the demo worktree (`StewartSquad-demo/`, :5180) to it, mark the approved numbers in `style-draft.json` (it is behind the round: the Hearth's `DRIFT`/`POOL`, the caves' tiers and stair, Home Wrong S4's placement, no `-03` frames listed).
3. **Round 2** (`REEL_FIXES_BRIEF.md` §2, with the §5 additions): intake is T-58 (the goblin totem's read), T-59 (Neve's hut roof open like a book — the LESSONS row claims it fixed, so first find which build the screenshot came from), plus whatever Andrew's look adds, plus the round-1 deferrals worth a brief (below). Small scene-local rounds may run on the Opus orchestrator per §1.
4. Then **"demo candidates"** (`DEMO_PROGRAM.md` §2) and the control build (`FABLE_CONTROL_BRIEF.md`); the rulings owed at the application step now include T-46 (Isabella's attack), T-57 (the rigs' handedness), T-39/T-40/T-36/T-37, the bible's missing penguin/seal/herd rows, Fern's stool and mist-weave.

## Known issues and load-bearing findings

- **Round-1 deferrals (full list: the audit's §3):** Isabella's 0.4 m dust trail (canon, needs a per-scene emitter); a *downward* look in flight needs `_shared/orbit.ts`'s pitch floor to be `min(18, station.pitch)` (T-23) — from CH's 8° station the look only tilts up; the look was framed on `CH` only; three hook-lamps stand inside the caves' stair band 1.4 m over the treads, the second stair meets the Depths floor with a lip up to 0.7 m, and a 0.1 m sliver of "tier" at the crescent's north tip; `rim-dawn S3` (0.34 m on W) and `shadow-wrong S2` (0.69 m on W) pass the walk probe on one direction only; the sprint is 1.2 × a flat 4.5 m/s (the walk has no kid identity); 8 frogs where the bible says 12; the caves' `L1` camera sits in the void and `W1` outside the shell by design; `shadow-wrong-round1-03-02` shows Isabella's hammer at hip height because she stands on a stump and the ±0.15 m `groundRel` clamp holds the head at her ground.
- **The rigs' `R` limb is the kid's left** (T-57): every named-hand prop is mirrored against the bible's hands. Flipping it re-shoots every frame; a design-lead call, not a fix-round change.
- **Driving a page from an agent:** unchanged — `scripts/sandbox-drive.cjs` with `?step=1`, `ssStep`/`ssSnap`/`ssKey`, 12 s after navigation, the plugin's `-NN` suffix; `ssKey` is keydown only (dispatch `keyup` yourself); expose probe state on `globalThis.ssProbe` (and `ssRigProbe` for the rigs). Agents must not start a dev server; if :5173 is down, the orchestrator restarts it with `preview_start`.
- **Parallel builders on one file worked** with region ownership stated in both briefs and "re-read immediately before each `Edit`, never `Write` the whole file" (`caves-descent/terrain.ts`: the shell loop vs the columns and stairs). Shared-doc appends (LOG, DECISIONS, LESSONS) from seven agents did not collide.
- **A `sed` that appends a comment to a line with a later statement comments the statement out** (`rig.ts:238` lost `ring.layers.enable` for one edit; caught by reading the line back). Read the line after any scripted one-line edit. **Long quoted heredocs fail through this session's shell** (twice); write long files with the Write tool and keep Bash for short appends.
- From before, still true: the Forest's own `walk.ts`/`liam.ts` duplicate the shared ones (both now carry the sprint and the curved ring); a kid hook must not add to an eased rotation; a saved frame right after load is blank (58,885 B); facing conventions (`180° − bearing` kids, `90° − bearing` +x creatures); patch TypeScript with a `.cjs` file; never `git add docs/design` wholesale.

## Handy paths

`sandbox/index.html` · `sandbox/_shared/` (scene, step, shot, rig, rock, kid-*, walk, creature, particles, water, rim, lantern, plane, biomes, style, material, sky, post, orbit) · the nine scene folders · `scripts/sandbox-drive.cjs` · `scripts/probes/` · `tests/unit/sandbox/` · `docs/qa/briefs/` · `docs/qa/rules/` (incl. `reel-fixes-03-audit.md`) · `docs/design/mockups/` (`README.md`, `LOG.md`, `LESSONS.md`, `STUDY_NOTES.md`, `DEMO_PROGRAM.md`, `REEL_FIXES_BRIEF.md`, `OPUS_FIX_PLAN.md`, `FABLE_CONTROL_BRIEF.md`, `style-draft.json`, the frames) · `docs/design/PHASE_0.75_TWEAKS.md` · `docs/design/PRE_BUILD_TODO.md` · `docs/reference/Screenshots/` (Andrew's) · `docs/LANES.md` · `docs/DECISIONS.md` · `docs/PROGRESS.md` · `docs/BRIEF.md`
