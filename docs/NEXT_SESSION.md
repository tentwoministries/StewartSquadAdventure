## Working Rules (apply to every session, every agent, every file)

These are Andrew's standing rules. They go at the top of every handoff document, every subagent system prompt, and `CLAUDE.md`. Subagents do not inherit the parent session's context, so each agent file must carry these verbatim.

1. **Missing inputs → stop and ask.** If a file, data source, or piece of context needed to do a task *properly* is missing, stop and ask Andrew rather than shipping incomplete work wrapped in disclaimers. Never paper over gaps. (Creative decisions are *not* missing inputs — see docs/BRIEF.md §2. Decide, log, proceed.)
2. **Pre-build gate for every external interface.** Before writing integration code against any API, package, or data format (Rapier, postprocessing, Colyseus, GLB loaders, save schema, Puppeteer GL flags), hit it with a real call and verify the actual response/shape. Never code against assumed or documented field names. Record the verified shape in the task's notes before proceeding. Non-negotiable.
3. **No assumptions in code.** Verify every field name, handler name, parameter, and format against the real source before writing tests, configs, or integration code. If unsure, grep the codebase first.
4. **Single Pro Inspection Checklist V2 — all 27 steps, every delivery, no tiers.** (Full list: docs/INSPECTION_CHECKLIST.md)
   - P0 pre-build: memory/handoff read, design approval on record, API verification done, inherited code audited
   - P1 static: `tsc --noEmit` clean, runtime import check, AST/import trace, duplicate definitions, TODO sweep
   - P2 semantic: call-chain trace, entry points, name collisions, fuzzy/near-duplicate logic, substring hazards, possessive/string hazards in content, router/state-machine transitions
   - P3 infra: priority ordering, data field coverage, async correctness, rate/frame budgets, deferred work listed, exclusions documented
   - P4 delivery: filenames, packaging, regression suite green, version stamp, line/size count
5. **Handoff is the source of truth.** Every session ends by writing `docs/NEXT_SESSION.md` with these Working Rules at the top, current state, what's done, what's next, and known issues. The next session starts by reading it.
6. **Deliverables are files, not chat.** Everything lands in the repo. Andrew never copy-pastes from chat.
7. **Tests: trim before adding.** Before generating new tests, do a trim pass on the existing suite; target ~100 tests per suite; propose specific trims with rationale before writing new ones.

**Family canon is sacred.** The kids' names, core personalities, and canon text never change: **Liam** (oldest, protective, steady — the leader), **Noah** (sharp, quick, independent), **Collette** (creative, imaginative), **Isabella** (youngest, fierce, unstoppable). Grandpa Ed flies the biplane. Each kid's color and role are set in the Design Bible (`docs/design/heroes.md`, Phase 0.5) and may be revised only there, with the rationale logged in `docs/DECISIONS.md`; Brief §1 is the starting point, not the law. Existing dialogue, quest text, in-jokes, cutscene lines, voice lines, achievement names, and loading tips are ported **verbatim** — never paraphrased or "improved". New text may be added. Nothing ships that isn't family-friendly.

## Routing (2026-09-08, after the reel fixes round 2): read docs/LANES.md first

This checkout is lane **0.75 visual studies** (the default lane, the demo lane) on branch `phase-0.75-visual-studies`. If Andrew's first line names another lane, move to that lane's worktree before reading further. Open lanes: 0.75 visual studies (this checkout, :5173), 0.75 demo (`StewartSquad-demo/`, :5180, **moved by Andrew's call to the untagged tip `c15f491` at the close of round 2**; `p0.75-demo-reel-2` is the tag to return to if wanted; `p0.75-demo-reel-3` is made when Andrew says the rounds read), the showcase lane (`StewartSquad-showcase/`, branch `phase-0.75-showcase`, :5184, opened by another session on 2026-09-08 for exploratory work; not touched by this session), 0.75 scratch (`StewartSquad-scratch/`, branch `phase-0.75-scratch`, behind), 0.85 story and play (`StewartSquad-story/`, branch `phase-0.85-story`), and, once its session cuts it, the Fable control (`StewartSquad-control/`, branch `phase-0.75-fable-control`, :5183). The Opus experiment lane is closed (branch kept at `59da9a6`). Default when unnamed: this lane. Opening phrases: "the reel fixes" / "my reactions" / a screenshots folder → this lane, `REEL_FIXES_BRIEF.md`; "demo candidates" → this lane, `DEMO_PROGRAM.md` §2; "control build" / "control review" → the control lane; "show the kids" → the demo worktree on :5180.

## Model and effort policy (Andrew, 2026-09-06; the delegated loop 2026-09-08)

- Orchestrator: Fable 5.1 at **high**, every session. At session start, confirm `CLAUDE_CODE_EFFORT_LEVEL` is unset (verified unset 2026-09-08, both sessions).
- `art-director`, `qa-inspector`, `design-lead`: `fable` / `xhigh`; the `-max` variants only for Phase 1 excellence-mark scoring and phase-gate reviews.
- Implementers and `sandbox-builder`: `opus` / `high`. `rules-librarian`, `rules-auditor`, `archaeologist` and mechanical work: `opus` / `medium`.
- **The delegated loop, proven twice** (`REEL_FIXES_BRIEF.md` §5 and §6): Fable diagnoses from the evidence, writes the briefs, reviews reports and frames, judges, commits; Opus agents build, audit and fix; a fix pass runs as parallel fixers, one per section; the orchestrator shoots the merged nine-frame sweep itself (`scripts/probes/round2-frames.cjs`, two minutes). Fable edits code only when the brief would be longer than the diff (T-59 this session: one sign).

---

# Next session — handoff written 2026-09-08 (end of **the reel fixes, round 2**; Fable orchestrated from Andrew's fourteen screenshots; three librarians, three builders, one auditor, three fixers; twelve commits from `bdd7cbc` to the close; **Andrew has looked at round 2's caves and meadow** (his second screenshot set became round 3's brief); rounds 1 and 2 still await his yes/no per item)

## Systems at a glance (the one-page map is `docs/design/mockups/README.md`)

| Piece | Where | State |
|---|---|---|
| Design Bible (not edited in 0.75) | `docs/design/*.md` | `p0.5-design-bible` on `main` |
| **The demo reel: nine scenes** | `sandbox/index.html` (tabs Places / Beats / Systems / Lab) · Places `forest-dusk/ desert-noon/ bog-night/ frozen-night/ caves-descent/ rim-dawn/` · Beats `shadow-wrong/ meadow-golden/` · Lab `flight-golden/` | **rounds 1 and 2 built on this branch, not yet tagged**; the demo worktree (:5180) still sits on `p0.75-demo-reel-2` |
| The shared runtime | `sandbox/_shared/scene.ts`, `walk.ts`, **`rig.ts`** (the ring now conforms to stepped ground per vertex: three radii, `aLift`, 0.17 ms for four kids), **`plane.ts`** (the open cockpit well cut flush in the taper, mirrored cabane struts, the pennant with `update(t, wind = 0)` and `solidsOf()`), `rock.ts`, `kid-*.ts`, `step.ts`, `shot.ts` | landed rounds 1 and 2 |
| The headless harness | `scripts/sandbox-drive.cjs` + `scripts/probes/*.cjs` (~80 probes; `round2-frames.cjs` is the nine-frame sweep) | every agent drives a page with it; the dev server must be up first (`preview_start` `vite-dev`, :5173) |
| Tests | `tests/unit/sandbox/{flight 13, place 11, rock 8, caves 7, goblins 7}.test.ts`, `version.test.ts` | **47 pass**; `npm run check` green at the close |
| The ledger | `docs/design/mockups/LESSONS.md` | §0 unchanged; rows added this round by each builder (Geometry: relief walked from the drawn columns; Rigs: the ring conforms, a carried prop is a thing in a hand, a rider sits in real geometry; Misc: a streamer is a pennant, a pack aims at slots) |
| Rules per task | `docs/qa/briefs/reel-fixes-{caves,meadow,flight}-04.md` + `reel-fixes-04-fixes.md` · `docs/qa/rules/reel-fixes-*-04.md` · `docs/qa/rules/reel-fixes-04-audit.md` (13 items, all judged) | the pattern for every round |
| Tweak rows | `docs/design/PHASE_0.75_TWEAKS.md` **T-01..T-68** | T-58, T-61..T-66 `built` this round; T-59 `fixed`; T-60 `fixed` (round 1's evening); **T-67, T-68 `observed`** (Ed's scarf at rest; the pennant's straight hang and the club under a walk-in) |
| The session record | `docs/design/mockups/REEL_FIXES_BRIEF.md` **§6** (round 2's run table), `docs/PROGRESS.md`, `docs/design/mockups/LOG.md` (seven new sections: the roof, three builds, three fix passes, the close) | |
| Frames | `docs/design/mockups/*-04-*.png` and `*-04b-*.png` (~80 this round); **`<scene>-round2-04b-01.png`** ×9 is the merged tree | committed |
| Andrew's evidence | `docs/reference/Screenshots/` (fourteen, committed; `vite.config.ts` ignores `docs/`) | |
| Branches / worktrees | `phase-0.75-visual-studies` (this; twelve commits `bdd7cbc`…close) · `phase-0.75-scratch` (behind) · `phase-0.85-story` · `phase-0.75-opus-experiment` (closed) · `main` untouched | |

## Where we are

- **Round 2 answered every item in Andrew's screenshots.** The caves have the chunky floor and the blocky stair back *with* round 1's behaviour (what is walked is what is drawn: the columns' quantized heights and the steps' treads are what `groundY` returns; `J` cycles flat / chunky / blocks; the ring conforms). The salamanders cling to lit wall and creep (four now). The goblin is a creature from the lore with two arms and a carried club, the pack fans out, the swing lands on the grass, the screen shake stays; the totems are war-posts with carved faces and braziers. The plane's "red line" (its rigid streamer bar) is a pennant; the kids sit in an open cockpit well cut inside the fuselage Andrew liked, with variants B (wing riders with straps) and C (Liam and Noah on the wing roots) on `N` and `?seats=`. Neve's roof is a gable.
- **Andrew must judge by looking** (two rounds now): the caves (S1, S2, walk the stair, `J`), the meadow (S3, press `0`, watch a hit; S4 the totem), the flight (`N` through A/B/C; the roll-out), the Hearth's roof, plus round 1's list (Home Wrong S4, the Bog's S2/W1 and Fern, the Hearth's drift and seals, the flight's look-around). Yes/no per item; a "no" goes to a fresh builder with what he saw. When it reads: tag **`p0.75-demo-reel-3`**, move the demo worktree (:5180) to it, mark the approved numbers in `style-draft.json` (it is two rounds behind).
- **The loop's second run confirmed the shape** (`REEL_FIXES_BRIEF.md` §6): screenshots make a better intake than a chat list because the cause is visible; parallel fixers halve the fix pass; the orchestrator shoots the merged sweep itself. Seams: a builder started its own dev server when the orchestrator's died (harmless, flagged in its report); a brief's framing (WG for the pennant) that nobody looked through first.
- **Toolchain:** `npm run check` green (tsc, eslint, 47 tests); `CLAUDE_CODE_EFFORT_LEVEL` verified unset. The dev server this session was started by `preview_start` and later by a builder's `npm run dev`; check :5173 before assuming either is up.
- **Design Bible:** unchanged (Phase 0.75 rule 1). Phase 0 part B still blocked on the brainstorm doc (Rule 1); the three legacy inputs still missing from `docs/legacy/`.

## What to do next session (in order)

1. Read `docs/LANES.md`, this file, `CLAUDE.md`, `REEL_FIXES_BRIEF.md` (§2, §5, §6), `LESSONS.md` §0, and the round's LOG sections for anything Andrew names. Confirm `CLAUDE_CODE_EFFORT_LEVEL` is unset. Start the dev server with `preview_start` `vite-dev` (check :5173 first).
2. **Andrew's look at rounds 1 and 2** on :5173 (the list above). A "no" becomes a round-3 row and a fresh builder. When it reads: tag `p0.75-demo-reel-3`, move the demo worktree to it, update `style-draft.json`.
3. **Round 3 is already briefed**: `docs/qa/briefs/reel-fixes-05-intake.md` (four items from Andrew's second set of screenshots in `docs/reference/Screenshots/TEMP SCREEN SHOTS/`, rows T-69..T-72: the caves' floating hook-lamps and the glow that rises with depth; Lamplighter Quartz rebuilt as a hooded figure; the totem's rag and tilted brazier; the goblin's club through its own head on the windup). Scene-local, two builders in parallel, about an hour; the brief ends with the prompt Andrew pastes. It may run on the Opus orchestrator (§1's default) and skip the audit unless `_shared/lantern.ts` changes. After it, further intake takes the same shape (`REEL_FIXES_BRIEF.md` §2 with §5 and §6's additions). Candidates already on the list: T-67, T-68, the fix pass's deferrals (Collette's robe 0.0065 m through the coaming skin, a shorter robe in `kid-collette.ts`; Liam's cape 0.03 m inside the lower wing in B/C; the inner hand's stall grab short by 0.11–0.14 m; the stair still floats over the void with no piers), and round 1's (Isabella's dust trail; the downward look in flight, T-23).
4. Then **"demo candidates"** (`DEMO_PROGRAM.md` §2), **P-10** (the chunky caves floor as traversal: a maze with few exits, jump-only paths, a lift spell — a story-lane gameplay item, from Andrew this session) and the control build (`FABLE_CONTROL_BRIEF.md`); the rulings owed at the application step now include T-46, T-57, T-39/T-40/T-36/T-37, the bible's missing penguin/seal/herd rows, Fern's stool and mist-weave, and **T-28 revised** (the four-read on the plane now comes from two rows and the rim, not 0.84 m across).

## Known issues and load-bearing findings

- **The caves' step limit is per relief setting** (`main.ts`: 0.60 m at flat/chunky, 1.10 at blocks) so a kid cannot drop over a channel wall; a fixed off-axis bearing now stops at the wall instead of descending — correct, and the walk probe records it. `groundY` on a tier is the highest drawn column top under the point (a point-in-hexagon test); on a stair it is the step's level tread. The ring's `aLift` steps at a column edge by design (not eased).
- **The plane's well** is cut from the fuselage's own taper (`WELL` in `plane.ts`; `solidsOf()` gives 92 solids for the mesh-level probe); rows sit 0.18 m apart across with per-kid z, hips 1.02 m, the rim at the shoulders (T-28's waist line is geometrically unavailable). The WG station cannot see the pennant (the upper wing covers it); `flight-golden-pennant-04b-05` is the readable framing.
- **The goblins** hold 0.55 m from the hero by damped inward travel (a band opening at 0.75 m, inside `BRAKE_TO` 0.8, so the loop's timing is unchanged to the digit); `HIT_HOLD` is 0.12 s; the body is built along +x with `rotation.y = bearing − 90°` in the meadow's `atan2(dx, dz)` convention.
- From round 1, still true: the rigs' `R` limb is the kid's left (T-57); the Forest's own `walk.ts`/`liam.ts` duplicate the shared ones (its ring gets no `aLift`, the T-41 residual); `ssKey` is keydown only; a saved frame right after load is blank (58,885 B); patch TypeScript with a `.cjs` file; never `git add docs/design` wholesale; long quoted heredocs fail through this shell (write long files with the Write tool).

## Handy paths

`sandbox/index.html` · `sandbox/_shared/` (scene, step, shot, rig, rock, plane, kid-*, walk, creature, particles, water, rim, lantern, biomes, style, material, sky, post, orbit) · the nine scene folders · `scripts/sandbox-drive.cjs` · `scripts/probes/` (`round2-frames.cjs` the sweep) · `tests/unit/sandbox/` · `docs/qa/briefs/` · `docs/qa/rules/` (incl. `reel-fixes-04-audit.md`) · `docs/design/mockups/` (`README.md`, `LOG.md`, `LESSONS.md`, `STUDY_NOTES.md`, `DEMO_PROGRAM.md`, `REEL_FIXES_BRIEF.md`, `OPUS_FIX_PLAN.md`, `FABLE_CONTROL_BRIEF.md`, `style-draft.json`, the frames) · `docs/design/PHASE_0.75_TWEAKS.md` · `docs/design/PRE_BUILD_TODO.md` (P-10) · `docs/reference/Screenshots/` (Andrew's) · `docs/LANES.md` · `docs/DECISIONS.md` · `docs/PROGRESS.md` · `docs/BRIEF.md`
