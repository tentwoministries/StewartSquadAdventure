# Task: the reel fixes round 2, the fix pass (`REEL_FIXES_BRIEF.md` §2 step 5)

Written 2026-09-08 by the orchestrator (Fable) after reading the three deliveries' frames and `docs/qa/rules/reel-fixes-04-audit.md`. Three fixers, one per section, in parallel; each touches only its section's files. The rule sheets stay the round's: `docs/qa/rules/reel-fixes-{caves,meadow,flight}-04.md`. Suffix `-04b` on every new frame. The orchestrator shoots the merged nine-frame sweep after all three report.

## A. The caves (files: `sandbox/caves-descent/*`, the ring block of `sandbox/_shared/rig.ts`, `tests/unit/sandbox/caves.test.ts`, `scripts/probes/caves-*.cjs`)

A1. **Chunkier steps (the orchestrator's read of `caves-descent-s2-04-01.png`).** The step is 0.60 m of arc and a 0.19 m riser: a fine ribbed stair. Andrew's "blockyness" is the reel-2 build's 0.42 m boxes. Make a step **1.20 m of arc** (`STEP_SUB` 4 → 8, or the equivalent), so the first stair rises about 0.38 m per step; keep the tread level and the riser vertical. Widen the test's riser band to 0.25–0.45 m. The walk must still descend: see A3.
A2. **The colours (audit 8).** Consecutive treads differ by about 5 % luminance at the study framing. Make the alternation read: the light tread `K.rockLight` lifted ×1.18, the dark tread `#4A4270` ×0.80, the riser face of each step a further ×0.7 of its tread, `jitterColor` ±0.05 kept. Check: at the study framing (`caves-descent-stair-04b`), sample the centre pixel of six consecutive treads; adjacent luminance ratio ≥ 1.25 on every pair. Quote the six.
A3. **The walk check that did not pass (audit 6).** On the two fixed-bearing ±40° runs Liam's y dropped 0.76 m in one frame: he stepped over the channel's side onto a tread far below, inside `walk.ts`'s 1.1 m `maxStep`. A stair's side wall is a wall. Set the caves' `maxStep` per relief setting in `main.ts`: **0.60 m at `flat` and `chunky`, 1.10 at `blocks`** (the relief pairs are ≤ 0.54 and ≤ 1.08). Re-run `caves-stair.cjs` (all nine runs) at `chunky`: max intrusion ≤ 0.05, **largest single-frame Δy ≤ 0.40** on every run (the new riser), Liam below −2.0 m in every run. If a fixed ±40° run now stops at the wall instead of descending, that is correct: report the final position and say so.
A4. **The ring's rim across a step (the caves builder's deferral a).** `RingGeometry(0.45, 0.84, 48)` has vertices at two radii only, so the rim at 0.62 ramps across a step by up to 0.25 m. Rebuild the ring with **three radii** (0.45, 0.62, 0.84; 147 vertices; `RingGeometry(0.45, 0.84, 48, 2)` gives exactly that: verify the vertex count against three r185 before relying on it) so `aLift` is sampled at the rim. Re-run `caves-ring-04.cjs`: worst residual at the rim vertices ≤ 0.06 m across a 0.54 m step; cost for four kids ≤ 0.5 ms (quote it).
A5. **Four salamanders (audit 12, `PHASE_0.75_ANIMALS_BRAINSTORM.md` §3).** Now that placement is a cast, place **two per stair**: the first pair on the first stair at arcs 6 and 14 (or the nearest arcs where the channel wall exists), the second pair on the second stair likewise; each on the side of the wall nearest a hook-lamp so the wall it lies on is lit. Re-run `caves-salamander-04.cjs` for all four (belly ≤ 0.06 m, closes ≥ 0.8 m). Update the HUD line and the creatures' count.
A6. **The salamander frame (audit 9).** `caves-descent-salamander-04-02` shows a pale animal on a black plane, which reads as floating: the complaint T-62 answers. Reshoot as `caves-descent-salamander-04b`: an `ssOrbit` framing from 2.5 m, pitch 10–15°, with **the lamp-lit wall, the wall's top edge and at least two treads in the frame**, the salamander mid-frame; read it in one line and say whether it reads as on the wall.
A7. Frames: `caves-descent-s2-04b` (the chunky stair, S2), `caves-descent-stair-04b` (the study framing), `caves-descent-s1-04b` (S1, the ring rim on a step: put Isabella astride a 0.54 m step edge for it), `caves-descent-salamander-04b`. `npm run check` green. LOG under `## Reel fixes round 2 — the caves, fix pass`; DECISIONS lines.

## B. The meadow (files: `sandbox/meadow-golden/*`, `tests/unit/sandbox/goblins.test.ts`, `scripts/probes/meadow-*.cjs`)

B1. **A goblin's body against the hero (audit 10).** `meadow-golden-hit-04-02` shows a goblin's torso through Isabella's shoulder. Add a hero push: a goblin's centre never comes closer than **0.55 m** to the hero's centre (hero radius 0.30 + goblin 0.25), applied the way the goblin-to-goblin push is, after the slot steering, eased (no pop). The windup trigger at `REACH` 1.0 and the brake at 0.8 are unchanged, so the loop's timing is unchanged; `goblins.test.ts` stays green. Extend `meadow-goblins.cjs`: over the whole run the min goblin-to-hero centre distance ≥ 0.55 and every state still reached for all three; quote both.
B2. **The colour table on the record (audit 11).** The reservation table lives only as a comment in `goblins.ts` 36–43 and omits the totem's `GT.shard` and `GT.coal`. Add the two rows to the comment and paste the whole table (all fourteen rows, hue/lightness/nearest hero/Δ/verdict) into the meadow's fix-pass LOG section.
B3. Frames: `meadow-golden-hit-04b` (the frame after a hit, no goblin body overlapping Isabella from S3), `meadow-golden-s3-04b`. `npm run check` green. LOG under `## Reel fixes round 2 — the meadow, fix pass`; DECISIONS lines.

## C. The flight (files: `sandbox/_shared/plane.ts`, `sandbox/flight-golden/*`, `tests/unit/sandbox/flight.test.ts`, `scripts/probes/flight-*.cjs`, `scripts/probes/plane-*.cjs`)

C1. **The well stays inside the plane Andrew liked (audit 4).** The well is 0.90 m wide with its floor at 0.62 m, so from the side the middle 2.8 m of the fuselage steps out and down from the yellow taper, with a soot trough under it (`desert-noon-parked-04-01`, `frozen-night-parked-04-01`). Make the well **flush**: at every x along it, the wall's outer face lies on or inside the fuselage's own half-width there (the taper 0.29–0.34 m), and the well's floor and belly never fall below the fuselage's own bottom line; the opening is a hole cut in the top of the same yellow shape, the rim its lip. The two kid rows sit at **z ±0.17** (hips 0.28–0.38 m wide fit in a 0.68 m well with a hand's width between). Check (new, `plane-outline-04b.cjs` or inside `flight-seats-04.cjs`): in plane space, sample the plane's silhouette from the left side (+z looking −z) and from below on a 0.05 m grid, `WELL` on and off; every silhouette pixel of the well version lies inside the version without the well plus 0.02 m. Quote the worst excess.
C2. **Nothing of a kid through the floor (audit 1).** Collette's dress hangs through the well's floor and out under the fuselage in every variant-A frame (`flight-golden-rollout-04-01` at about (735, 598)). `clearance()` returns positive below the floor, and the probe samples bones, not meshes. Extend the probe: for every kid, transform **every vertex of every mesh under the kid's root** into plane space (a few thousand points; once per variant per frame is fine) and assert none lies inside any solid box **and none lies below the well's floor while inside the well's xz footprint**, and none inside the wings. Then fix the pose or the seat so it passes in A and C (raise the hips, shorten what hangs, or seat the girls so the dress lies on the floor; say which), and B (nothing through the wing). Quote the worst signed clearance per kid per variant, mesh-level.
C3. **The four-read at the ride's station (audit 5, T-28).** With the flush well the rows sit 0.34 m apart across. The four-read comes from the two rows and the rim now, not from T-28's 0.84 m: check it. At CH at 18 s in variant A, project the four heads' centres to the screen: every pairwise distance ≥ 0.6 of a head's projected width, and no head more than half hidden by another kid's mesh (a ray from the camera to each head centre hits that kid first). Quote the four. If it fails, move the rear row back or up until it passes, inside C1.
C4. **The trailing pennant frame (audit 2).** Shoot `flight-golden-pennant-04b` at **WG at 18 s** (`?shot=WG&t=golden`, step to 18 s): the pennant trailing aft off the strut at wind 1. Read it in one line.
C5. **`onWing()` by float equality (audit 13).** Give each seat an explicit `kind: 'well' | 'wing'` and read that.
C6. Frames: `flight-golden-rollout-04b`, `flight-golden-seats-a-04b` (front-left quarter, the flush well, the dress on the floor), `flight-golden-ch-a-04b`, `flight-golden-pennant-04b`, and the three parked reads `bog-night-parked-04b`, `desert-noon-parked-04b`, `frozen-night-parked-04b` (the flush well from the side: no step, no trough). `npm run check` green. LOG under `## Reel fixes round 2 — the flight, fix pass`; DECISIONS lines. Commit nothing; the untracked `scripts/probes/plane-04-frames.cjs` (audit 3) is the orchestrator's to add.

## Shared docs

Append only, one edit per file, re-read first; three fixers run at once.
