# Rule sheet — the Crystal Depths, reel fixes round 2 (`docs/qa/briefs/reel-fixes-caves-04.md`)

Written by `rules-librarian`, 2026-09-08. Tier-1 rows per `DEMO_PROGRAM.md` §1. Rows: **T-61, T-62**, plus round-1 deferrals (b) and (e) from `docs/qa/rules/reel-fixes-03-audit.md` §3 "Caves".
**Gap (Working Rule 1), carried from `reel-fixes-caves-03.md` line 4:** `dungeons.md` §2.7 is a *preview* — light rule, wall-lamps, rim crystals, no tiers, no stairs, no columns, no relief. Every geometry number here is **T-18 (`proposed`)** or the demo's. Likewise `heroes.md` §2.7.5 gives the ring's radii and its 0.01 m `polygonOffset` but says nothing about stepped ground: the +0.04 m raise and the per-vertex conform are **T-56/T-61**, not bible. Say both in the report; do not revise the layout.

## 1. Applies

**Never-run rule first.** `LESSONS.md` §0 rule 8: "a mechanic is not built until it has been seen to run in a stepped probe, every state reached." Three mechanics here: the `J` relief cycle (all three of `flat`/`chunky`/`blocks` stepped, plus the URL form), the salamanders' `still`→`climb` (probe 4), and the ring's per-frame `aLift` (probe 3). The tests do not discharge any of them.

- **Always · `LESSONS.md` §0 rule 10 (the `_shared/` gate)** — "a change under `sandbox/_shared/` is checked with one frame per scene." You *are* changing `rig.ts`, so brief check 6's nine `<scene>-ring-04` frames are mandatory, each read against `<scene>-round1-03-02.png`. Audit §1 item 7 is why the baseline is the merged sweep: every earlier sweep was shot against a tree that changed under it.
- **`LESSONS.md` Rigs row 11 + T-41 (`built`, round 1) + T-27** — "anything that sits in the world takes the world's bend … driven by the *same* shared uniform objects, passed by reference and never copied." `aLift` is added to world y **after** the curve term, and the ring's existing `uCurve`/`uCurveCenter` references stay by reference; a copied uniform re-breaks the reel-2 defect Andrew named ("the blinking light under their feet moves up their bodies").
- **T-41 residual (audit §3, shared (g))** — `sandbox/forest-dusk/liam.ts`:118 builds the Forest's *own* ring on the unbent shader; a `rig.ts` fix does not reach it. If `forest-dusk-ring-04` differs, it is this row: report, do not fix (not in your file list).
- **T-56 (`built`, round 1) + `heroes.md` §2.7.5** — "a walkable surface's rendered tops agree with `groundY` to within 0.05 m; a ground decal sits at ground + 0.04 with a polygon offset and is checked on the scene's *stepped* ground." Audit §1 item 1 (**Sure**): `rig.ts`:238 still reads `ring.position.y = 0.02` — neither round-1 builder lifted it. Lift it to 0.04 in this task and record the departure from §2.7.5's 0.01 m. Canon the probe must not break: outer diameter 1.6 m, 35 % alpha at 0.55 m to 0 at 0.80 m, crisp rim 0.06 m wide at **0.62 m** (the brief's probe radius), additive, `depthWrite false`, breathing ±6 % at 1.2 s — so `aLift` is sampled from each vertex's *current, breathed* world xz, not its rest position.
- **T-55 (`built`, round 1)** — "where a stair crosses a tier, the tier's geometry is cut to the stair … the drawn and walkable widths agree." Round 1's `cutAt`/`lowTread`/shoulder logic survives the relief and the blocky stair; a relieved column may not top out above the lowest tread its body reaches.
- **`LESSONS.md` §0 rule 3** — "nothing pops: every state change eases over ≥ 0.15 s." The `J` rebuild re-grounds the kids (`root.position.y = groundY`) — that is a teleport; ease it or state in the report that it is a debug key outside the rule. `aLift` steps as a kid crosses a column edge: say whether the step is eased or is the point.
- **`LESSONS.md` Rigs row 8 (penguins/seals) + row 9** — "a lie-down, a pitch or a roll is about the body's own axis: set `rotation.order = 'YXZ'` … check it by reading the *world* direction of the local axes in a probe, not the frame." A salamander lying belly-to-wall is a full orientation on a non-vertical surface: build the basis (−y = wall normal, +x along the wall) as a matrix or an ordered Euler, and read its world axes in probe 4.
- **`LESSONS.md` Rigs row 6 / T-26 / `STUDY_NOTES.md` §6 rule 3** — a `+x`-built creature takes `rotation.y = 90° − b`, and every accumulating heading is wrapped to [−π, π]. The re-cast each climb step accumulates a heading; wrap it.
- **`LESSONS.md` Density row 4 ("a deer stood in mid-air")** — anything placed near an edge asserts `inside()`; a surface is found by **marching/casting**, not by subtracting a radius. This is exactly T-62's cast-not-typed-point rule; the fallback ("a column's side face at the crescent's edge") takes the same cast.
- **`LESSONS.md` Camera row 4 (its example is this scene)** — "a station in an enclosed space is checked against the enclosure's geometry"; `caves-descent/main.ts` `L1`/`W1` once rendered black inside a tier's rock. `blocks` doubles the relief to ±1.08 m: re-check every station at all three settings, and derive any new station number from a saved frame (**T-30**).
- **`dungeons.md` §2.7 + `LESSONS.md` Light row 6 / T-10** — "wall-lamps on hooks **along the stair** and the main paths", emissive-only, `dark 0.72`, **no torches**, rim crystals 2× within 6 m. Deferral (b)'s three lamps move sideways along the stair, keep cd 0, add no point light, and the scene stays under ~20.
- **Round-1 deferral (c), a regression check** — a rim crystal grew through the tread in `caves-descent-stair-03-01.png` and was cleared by `stairY(x, z) === null` in `props.ts`. The blocky stair moves every tread; re-assert it.
- **`PHASE_0.75_ANIMALS_BRAINSTORM.md` §3, Crystal Caves** — the cave salamander: "**4**", pale `#E8DCC4` with `#6CE87A` spots "that pulse on the crystal heart's oscillator; **clings to walls**; slides into pools", reaction "freezes; if a hero stands still 3 s it climbs 1 m closer". The demo has 2 and the brief keeps 2 — record the shortfall; keep the spots on the heart's one 8 s oscillator (T-18) and do not re-time the 3 s / 1 m.
- **`LESSONS.md` Process rows 1, 2 + §0 rules 9, 11, 12 / T-29** — wait 10–12 s after navigation (a blank save is **58,885 bytes**); patch TypeScript with a `.cjs` file, never an inline `node -e` with backticks; step with `ssStep`, never wait for a screen; `?t=half` explicit on every save, names lowercase.
- **`LESSONS.md` Process row "the songbirds never fired"** — a trigger on a distance uses `<=`, or the placement sits clearly inside it. Boundary-dense here: the point-in-hexagon test "within one grid pitch", the 0.54 m adjacency limit against `maxStep` 1.1, the 35–65 % histogram bounds, the 0.12–0.30 m riser band, `lowTread(x, z) !== null`. State each comparison's inclusivity.
- **`LESSONS.md` §0 rule 5 / T-08** — "a station is a composition: look through its lens before saving it, and keep its line of sight clear." Six named frames plus the sweep; brief check 5 asks each read to say whether the floor reads as the old screenshots.
- **Working Rule 7 + audit §1 item 3 (`Likely`: no trim pass ran last round)** — the suite is **39** tests, cap **≤ 5** new. Name the trims considered and rejected, with the reason, before adding.
- **`PRE_BUILD_TODO.md`** — the file's own rule: "Items move to `docs/SESSION_PLAN.md` when scheduled and get a `DECISIONS.md` line when done or dropped." Fix 5's row is the next free id, **P-10** (P-01..P-09 are taken), lane "0.85 story and play", status `open`; **P-05** ("the caves' stairs and the rock") is this task's parent and stays open until Andrew's pass.

**Open observations (status `observed`, `DEMO_PROGRAM.md` §1 rule 4 — not constraints):** **T-61**, **T-62** (your two fixes; their "Rule" column is the definition of done, not a rule to honour elsewhere), **T-34** (their grandparent), **T-57** (the rig's `R` limb is the kid's left — you touch `rig.ts`, but only the ring; leave it), **T-63..T-66** (other scenes, round 2).

## 2. Checks implied

1. **Stepped probe before any test is trusted** (never-run rule): `J` cycled through `flat`→`chunky`→`blocks` and back under `?step=1`, the HUD line quoted at each, plus `?relief=flat|chunky|blocks`; and after each change no kid is inside rock (`root.y − groundY` per kid, all four).
2. **`J` is unbound** — grep `_shared/scene.ts` for the runtime's keys (T, V, K, P, F, U, O, B, X, H, Tab, Enter, `,`, `.`) and quote the check before binding.
3. **Ring conform** (probe 3, T-41/T-56): `|aLift + root.y + 0.04 − top| ≤ 0.06` at 12 vertices at radius 0.62 over 20 points, worst quoted; sampled at the *breathed* radius; and the per-frame cost mean over 120 frames **< 0.5 ms** for four kids.
4. **Ring `y` = 0.04** in `rig.ts` (audit item 1 closed), with `heroes.md` §2.7.5's 1.6 m / 0.55 / 0.80 / 0.62 / 0.06 numbers unchanged and the departure from its 0.01 m offset stated.
5. **The `_shared/` gate** (§0 rule 10): nine `<scene>-ring-04` frames vs `<scene>-round1-03-02.png`; on flat ground nothing but clock phase differs. `forest-dusk` differing is T-41's residual — report it.
6. **The walked surface is the drawn surface** (T-55/T-56): brief test (a) 400 points, raycast vs `groundY` within 0.05 m *and* every point covered by a column; (b) both stairs a staircase, risers 0.12–0.30 m, never negative descending, `stairY` within 0.05 m of the highest hit; (c) no 2.2 m pair differing by more than 0.54 m; (d) the histogram 35–65 % at 0.
7. **Walk probe** (`caves-stair.cjs`, the never-run rule for the stair): three bearings, `w` 240 frames, Liam below −2.0 m, max intrusion **≤ 0.05 m**, largest single-frame Δy **≤ 0.30 m**.
8. **Salamander probe** (probe 4): belly cast hits `stairs`/`tiers` within **0.06 m** at rest and every 10 frames of a 240-frame climb; ≥ 0.8 m closer over the run; plus the **world** directions of its local −y and +x at three climb steps (Rigs row 8), and the heading wrapped.
9. **Deferral (b):** the three lamps at `[-20,-31]`, `[-27,-25]`, `[-33,-17]` now ≥ 0.4 m outside the band, still along the stair, cd 0, point-light count quoted (`dungeons.md` §2.7).
10. **Deferral (e):** the second stair's foot lip **≤ 0.15 m**, sampled across the 3 m blend, at all three relief settings.
11. **Regression:** no rim crystal through a tread on either rebuilt stair (round-1 deferral (c)); and stations `L1`/`W1`/`S1`/`S2` not inside rock and not outside the back-face shell at `flat`, `chunky` and `blocks` (Camera row 4).
12. **Frames:** the six `-04` names, `?t=half` explicit, lowercase, ≥ 12 s after navigation, none 58,885 bytes; one line each saying whether the floor reads as the "Old way it was done" screenshots.
13. **`npm run check` green**, suite count stated, the trim pass named (Working Rule 7); `PRE_BUILD_TODO.md` gains exactly one row, id P-10.

## 3. Not applicable, considered

- **T-43** (non-indexed `IcosahedronGeometry`, displace per unique position) — round 1 moved the caves' shell onto `_shared/rock.ts`; the relief is column *tops*, not vertex displacement. Applies only if you add a jittered rock; then it binds in full.
- **T-44 / T-48 / T-60** (sprint lock, the flight look-around, the flourish veto) — `rig.ts` and `walk.ts` rows from round 1, but none touches the ring block; the brief bars the rest of the file, and `walk.ts` is not yours (`maxStep` is read, never changed).
- **T-01 / T-02** (the caves as a fifth biome; lamps as a growth mechanic) — application-step design rows; the fourteen hook-lamps are set dressing here and deferral (b) only moves three.
- **T-24 / T-39** (the 12 % luminance floor, the far fill) — `shadow.wrongDusk` rows; the caves' darkness is `dng.cave` 0.72 and this pass is geometry.
- **T-09 / T-22** (scatter density, litter colour) — the caves' floor is columns, not tufts; no scatter changes.
