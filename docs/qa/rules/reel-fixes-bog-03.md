# Rule sheet — the Bog: frogs and Fern (`docs/qa/briefs/reel-fixes-bog-03.md`)

Written by `rules-librarian`, 2026-09-08. Rows: **T-49, T-50**. Tier-1 fetch per `DEMO_PROGRAM.md` §1.

## 1. Applies

**Never-run rule first.** `LESSONS.md` §0 rule 8: "A mechanic is not built until it has been seen to run in a stepped probe, every state reached." The frog hop (seat → choose free pad → arc → land → flee-jump) is a keyed/timed mechanic: it is done only when the 120 s stepped probe shows every state, not when `creatures.ts` reads right. `LESSONS.md` Process row "The goblins never attacked, and the log recorded that the scene 'read first time'" is the same defect one scene over.

- **Always · `LESSONS.md` §0 rule 9 + Process row 5** — "The clock stalls on a hidden page: step the runtime, never wait for a screen." Every check here (`ssStep(7200)`, `ssStep(120)`, `ssStep(200)`) depends on it; read `ssWorld.hud()`/`ssProbe`, not the DOM.
- **Always · `LESSONS.md` §0 rule 12** — "Patch TypeScript with a `.cjs` script file, never an inline string with backticks." Applies to any scripted edit of `creatures.ts` / `flora.ts` / `props.ts`.
- **Always · `LESSONS.md` §0 rule 10 + brief "Files you may touch"** — a `_shared/` change costs one frame per scene; this pass is scene-local (`sandbox/bog-night/`), so its own frames suffice. Do not reach into `_shared/` or `terrain.ts` to solve the pad graph.
- **Always · `LESSONS.md` §0 rule 11 + T-29** — "`?t=` explicit on every save; frame names lowercase." Every `-03` frame carries `?t=night`; the runtime still defaults `t` to `'dusk'` (T-29 unfixed as a design row).
- **Always · `LESSONS.md` §0 rule 3 + T-06 (`MOTION_TEMPO_NOTES.md`)** — "every state change eases over ≥ 0.15 s, every recurring motion runs on two incommensurate rates." Fern's 0.04 rad sway is exactly this row; so is the jump's ease (fast start, slow arrival, STUDY_NOTES §6 row 4) and the lantern pendulum.
- **Always · `LESSONS.md` §0 rule 4 + T-26 + STUDY_NOTES §6 row 3** — a kid rig's front is local +z (`180° − bearing`), a +x-built creature `90° − bearing`. The frogs are `creature.ts` rigs (+x) and Fern is a built figure: her `yaw(160)` must be re-checked against the rebuilt mesh's front axis, not assumed to survive the rebuild. Two close-ups shipped backwards in session 2.
- **`LESSONS.md` Rigs row "A creature turned the long way round"** — "wrap every accumulating angle to [−π, π] each step; take the short way." A frog that now turns toward a chosen pad accumulates a heading.
- **`LESSONS.md` Rigs row "Collette's orb sat at her chest; Isabella's hammer lay flat"** — "a planted or resting prop is parented to the root (or the ground) and the hands are posed to it; a carried prop is parented to the hand." Fern's hooked staff is planted: parent it to her root and pose the right hand onto it (the same defect T-42 raises for Collette's staff).
- **`LESSONS.md` Density row "A deer stood in mid-air over the void"** — "anything placed near the plate's edge asserts `inside()`; a bank is found by marching, not by subtracting a radius." Seats and jump targets are chosen from the *rendered* pads, and any pad added in `flora.ts` is asserted on water/ground, not computed from a radius.
- **`LESSONS.md` Camera row "A landmark filled the frame or blocked the station's subject"** — after placing props, look through every station that faces them; the Bog's `flora.ts` already carries a `clear()` radius round the hut. Added pads near the causeway must not enter S1's or S4's foreground.
- **`world-events-weather.md` §2.7.1 Bog row (canon)** — frog: count **12**, silhouette "the sit-and-jump", habitat "reeds, lily pads", **flee radius 3 m**, "jumps into water with a `pt.bubble` ring". The brief's eight frogs are below the bible's twelve (a demo count, not a violation); the 3 m flee radius is the number the brief's walk-up check must use, and the landing ripple is the canon `pt.bubble` ring — keep it.
- **`world-events-weather.md` §2.7.1 shared rules** — idle "graze/idle 4–12 s, then walk … repeat"; flee "5 m/s for 4 s away from the threat, then resume idle". The 20 s no-jump guarantee (T-49) is the demo's reading of that cadence; state it in the report as the demo's number, not the bible's.
- **`PHASE_0.75_ANIMALS_BRAINSTORM.md` §2 and §3 (Bog)** — three-layer rule: floor / **one signature** / **one reaction**. The frog's signature is "sit-and-jump with a bubble ring (bible)"; its reaction is "★ one croaks back when the `Sugar?` emote plays". The pad-occupancy fix is the floor working properly; do not add a fourth behaviour while in there.
- **`npcs.md` §2.6.5 Mistweaver Fern (canon, design law)** — silhouette "hunched (**1.55 m**) under a fringed shawl whose fringes hang to the knee and move; a **1.9 m** hooked staff with an amber lantern swinging from the hook"; palette `#7FA68F` shawl 50 % / `#4A3524` dress 30 % / `#70C090` fringe tips and staff binding / lantern `#FFB347`; face "old, sharp, amused; a long braid of grey"; post "the jetty's end at (0, 0), **a stool**"; light "a placed `light.lantern` (`#FFB347`, 1.0, 4 m) on a 1-bone pendulum (0.9 rad/s)"; motion sources "the lantern pendulum; her weaving hands (two `pt.wisp` particles per second …); a slow rock on the stool (0.4 rad/s); the fringes"; read at distance "a hunched fringe under one swinging amber light at the end of a line of lights". The brief's "about 1.5 m" and its reed-fringe hem are inside this row; build to **1.55 m**, keep the shawl fringe reading, keep the lantern at 1.0 / 4 m (brief: light budget unchanged), and note in the report that the demo figure stands where the bible seats her on a stool and weaves with her hands — the sway substitutes for the stool rock (0.4 rad/s) and the `pt.wisp` weave is deferred, not dropped.
- **`npcs.md` §1.3 table row (canon)** — Fern 1.55 m (hunched), guide, swamp chain, hooked staff 1.9 m with a hanging amber lantern, `ui` `#70C090`; §4.1 budget: a guide rig ≤ 1,000 tris with props ≤ 200, LOD tier 3. Two arms, a face and a hood must land inside that budget.
- **`npcs.md` §3 (v27 §19.1) — the shared torch oscillator** — "Quartz's pole-lamp, **Fern's hook-lantern**, Neve's hearth-pot and the seven escort posts each run one oscillator that drives flame shape and light intensity together." Do not desync the lantern while rebuilding the arm that holds it.
- **`npcs.md` §3 (v27 §19.2)** — "A standing guide reads as a person and not a signpost for the same reason the v27 ground read as a toy set: the count" (every NPC idle has two or more incommensurate sources). This is the *why* of T-50, in canon, and the acceptance bar for the close-up.
- **`npcs.md` §2.6.5 / §2.9.5** — the escort frog "hops onto the jetty and lives there" after `swamp_2`; the 30 s idle has it hop to her. The pad system must not later claim the jetty frog as a pad-dweller — note the seam in the report.
- **`LESSONS.md` Light row "Frame rate dipped … each lantern carried a point light"** — "most lanterns are emissive-only; keep a scene under about twenty point lights." The brief's "lantern's light budget unchanged" is this row.
- **T-47 (`PHASE_0.75_TWEAKS.md`, observed → this round)** — the Bog's S2 and W1 are unwalkable placements, owned by `reel-fixes-stations-03.md`. If your flee-jump probe walks Collette from a station and she cannot move, that is T-47's task, not yours: report it, do not fix `main.ts`.
- **T-41, T-42, T-43 (observed → sibling briefs this round)** — the selection ring's curve, Collette's staff and the rock displacement are fixed in `reel-fixes-shared-03.md` / `-rocks-03.md`. Your Bog frames will show all three; read them but do not touch them.
- **`PRE_BUILD_TODO.md` P-04, P-07** — the demo-candidates table and the 0.75 application step consume this round; anything deferred (the `pt.wisp` weave, the stool, frog count 8 vs 12) is written down in the report, not left implicit.

**Open observations (not rules; status `observed`, `DEMO_PROGRAM.md` §1 rule 4):** T-40 (the whirl ribbon's colour), T-34/T-55/T-56 (the caves), T-51..T-54 (the Frozen scene) — other scenes, no constraint here.

## 2. Checks implied

1. **Every frog state seen, stepped.** `bog-night/?shot=S1&t=night&step=1`, `ssStep(7200)` sampling every 60 frames via `ssProbe.frogs()` (position, pad index, jump count, airborne): quote min/max jump count, the longest inter-jump gap, the longest jump distance. (never-run rule; §0 rule 9)
2. **No two seated frogs within 0.5 m at any sample; every seated frog within 0.25 m of a real pad centre.** Quote the worst pair distance and the worst pad offset. (T-49; the pads must be `flora.ts`'s rendered pads, not coordinates)
3. **Jump ≤ 4.0 m, gap ≤ 20 s, count ≥ 4 at 120 s**, and every seat's pad has ≥ 2 pads within 4.0 m — assert the pad graph's connectivity in the probe, not by eye. (T-49)
4. **Flee at the canon radius.** Walk Collette in (`w`, stepped) and confirm the jump fires inside the bible's **3 m** flee radius within 1 s, with the `pt.bubble` landing ring present. (`world-events-weather.md` §2.7.1 Bog row)
5. **Headings wrapped:** assert no frog's heading leaves [−π, π] and none turns the long way over the 120 s run. (`LESSONS.md` Rigs row)
6. **Fern reads as a person.** `bog-night-fern-03` (ssOrbit, head target, d ≈ 5, pitch 12) and `bog-night-s4-03` with `ssStep(120)`: one line each naming head, face, hood, two arms, the staff *in* a hand, the lantern on the hook. (T-50; `npcs.md` §2.6.5 "read at distance")
7. **Numbers quoted:** her bounding-box height against the canon **1.55 m**, the staff's length against **1.9 m**, and the max sway lean in rad over 200 stepped frames (target 0.04, two incommensurate rates). (T-06, §0 rule 3, `npcs.md` §2.6.5)
8. **Facing:** confirm from `bog-night-fern-03` that she faces the causeway at `yaw(160)` after the rebuild — the frame, not the guess. (T-26, §0 rule 4)
9. **Lantern unchanged:** grep the light's colour/intensity/distance before and after and state them (`#FFB347`, 1.0, 4 m equivalent) and the scene's point-light count. (`npcs.md` §2.6.5; LESSONS Light row)
10. **Frames:** `bog-night-s1-03`, `bog-night-l1-03`, `bog-night-s4-03`, `bog-night-fern-03` — lowercase, `-03`, `?t=night`, card on, saved ≥ 12 s after navigation; none is 58,885 bytes. (§0 rule 11, Process row 1, T-29)
11. **Sightlines:** if any pad is added in `flora.ts`, read S1 and S4 and confirm nothing new sits in the station's foreground or over the plate's edge. (Camera row 4, Density row 4)
12. `npm run check` green; `tsc --noEmit` silent for `sandbox/bog-night/`; the `main.ts` edit, if made, is the single `makeCreatures(...)` line and nothing else in the diff.

## 3. Not applicable, considered

- **T-16 (Bog fog)** — the night fog pair is already applied in the scene; this pass changes no keyframe, and the close-up is inside the near range.
- **T-14 / brainstorm §2 "one signature, one reaction"** — the ★ `Sugar?` croak-back and the will-o'-wisps are new behaviours for the application step; the brief fixes the floor and adds nothing.
- **T-44 / T-48 (sprint lock, CS-04 look-around)** — other scenes' new features this round; the Bog probe uses plain `w`.
- **T-38 / T-53 (a waterside needs a bank; a sheet of water lies in a basin)** — the pads are props on existing water, not new shoreline or a new pool; if a pad is added over the plate's edge, T-38's shore rule comes back.
- **T-24 / T-39 (the Shadow Realm's luminance floor)** — a `shadow.wrongDusk` row; the Bog has its own keyframe and no luminance probe is asked for here.
