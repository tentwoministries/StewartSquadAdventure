# Rules audit — the reel fixes, round 2 (the caves, the meadow, the flight)

`rules-auditor` (Opus), 2026-09-08. Delivery: `phase-0.75-visual-studies`, commits `4b54d29` (caves), `0b3c3ec` (meadow), `e8674d9` (flight), plus the untracked `scripts/probes/plane-04-frames.cjs`. Read against the whole of `docs/design/mockups/LESSONS.md` (§0 and every table including Misc), `docs/design/PHASE_0.75_TWEAKS.md` (T-01..T-67), `docs/design/PRE_BUILD_TODO.md`, `STUDY_NOTES.md` §6, the three briefs and the three rule sheets, the three `LOG.md` reports, the diffs, the probes and the frames.

Excluded by instruction (already with the fix pass): the caves' 0.60 m step arc / 0.19 m riser, and the ring's rim at r = 0.62 ramping across a step (caves deferral (a)).

Verified here, on the merged tree: `npm run check` — `tsc --noEmit` clean, `eslint` clean, vitest **44 passed / 6 files**.

## 1. Suspected misses

1. **Sure · a violet part of Collette hangs through the well's floor and out under the fuselage.** `flight-golden-rollout-04-01.png` at (735, 598) = `#5515ae`; `flight-golden-seats-a-04-03.png` at (933, 663) = `#4b12a1`; `flight-golden-pennant-04-02.png` at (765, 735) = `#5615ae` — a saturated violet wedge against the `#050302` underside of `well.floor`, present in every variant-A frame and absent from the three parked frames (no kids aboard). Rule: `LESSONS.md` Misc, "a rider sits *in* or *on* real geometry … a stepped probe transforms hips, both knees, both feet, both hands and the head into the vehicle's own space". The probe samples eight **bone** points and no body mesh, and `clearance()` in `sandbox/_shared/plane.ts` (~line 112) returns a *positive* number for any point below `well.floor` (y < 0.62), so a dress or a boot that has passed through the floor and out the bottom scores clear. The report's "worst signed clearance +0.030 m, every one ≥ 0" is true and does not cover this.
2. **Sure · the brief's pennant frame was never shot.** `docs/qa/briefs/reel-fixes-flight-04.md` check 4 asks for `flight-golden-pennant-04` = "**WG at 18 s**: the pennant trailing off the strut". `scripts/probes/flight-frames-04.cjs`:70 shoots it at `?shot=LD&…` at **t = 26.0 s** (the landing beat), and `-rollout-04` at 28.45 s — both at or near zero airspeed. There is therefore **no saved frame of the pennant at wind 1 trailing aft**; T-65's *trailing* half exists only as probe numbers (worst Δx −1.034 m over 240 frames). `LESSONS.md` §0 rule 5 and the brief's "read each in one line" are discharged on the hanging state only.
3. **Sure · `scripts/probes/plane-04-frames.cjs` is untracked.** `git status --short` after all three commits still lists it, and the flight's `LOG.md` names it as the script that rendered the nine-frame `_shared/` sweep. P4 (packaging): the sweep's evidence is committed, its instrument is not.
4. **Likely · the plane's outline changed where the brief said keep it.** `reel-fixes-flight-04.md` fix 2: "Keep the footprint, the cowl, the wings, the struts … the plane's outline in the WG station must be the plane Andrew liked." `sandbox/_shared/plane.ts` `WELL.halfZ` 0.45 and `WELL.floorY` 0.62 against a fuselage taper of 0.29–0.34 m half-width over x −2.10 … +0.70, so the middle 2.8 m now steps **out and down** from the nose and tail sections. `desert-noon-parked-04-01.png` and `frozen-night-parked-04-01.png` both show a black-bottomed rectangular trough wider and lower than the yellow taper, with a visible step at the aft bulkhead. The DECISIONS line admits "the plan view widens 0.16 m a side"; it does not say the soot belly and the two steps read from the side, and the report's "same radii, same outline" is true only of the two end sections.
5. **Likely · T-28's four-read is not honoured and nothing checks it.** `PHASE_0.75_TWEAKS.md` T-28: "the demo seats them **0.84 m apart across** and 0.72 m along … which is what makes four kids read as four." `sandbox/flight-golden/flight.ts` `WELL_Z = 0.16` seats the rows **0.32 m** apart across. `flight-golden-seats-a-04-03.png` shows the four heads overlapping into two-and-a-bit shapes with the rim at their chins. Flight deferral (a) discloses the change against the *brief's* ±0.34 m and deferral (b) against the waist/shoulder line; neither cites T-28's 0.84 m, and no acceptance check tests head separation.
6. **Likely · the caves' walk-probe acceptance check did not pass as written.** `docs/qa/briefs/reel-fixes-caves-04.md` check 2: "the largest single-frame change in his y (must be **≤ 0.30 m**: a step, not a fall)". `LOG.md` check 2 records **0.7619 m** on the two fixed-bearing ±40° runs, argued away as within `walk.ts`'s own 1.1 m `maxStep`, with 0.1905 m quoted from re-aimed runs instead. A named check that fails on 2 of 9 runs and is reported as an explanation rather than as a failure needs the orchestrator to decide whether the check or the geometry moves.
7. **Likely · the flight's `_shared/` gate is discharged by assertion, not by the diff.** `LESSONS.md` §0 rule 10. The flight's `LOG.md` reads: "none of the nine scenes' own stations has the plane in frame, so the sweep proves nothing regressed." No pixel diff was run on the nine `<scene>-plane-04-01.png`, and md5 shows 8 of 9 are **byte-identical** to the caves builder's `<scene>-ring-04-01.png`, with `caves-descent-plane-04-01` == `caves-descent-s1-04-01` and `meadow-golden-plane-04-01` == `meadow-golden-s1-04-03` — the flight's gate frames are the other two builders' own scene frames. The plane's only real evidence is the three `-parked-04` close frames, which have no round-1 baseline to be read against.
8. **Worth a look · T-61's "the colors" half barely reads.** `caves-descent-stair-04-01.png`, consecutive treads sampled at x = 700: `#100931` / `#170f45` / `#090a47` / `#000e4c` / `#030b44` / `#080631` — the per-step alternation between `K.rockLight` and `#4A4270` is in the data but under about 5 % luminance apart at the study framing, so Andrew's "the blockyness **and the colors**" reads as blockyness only. Sibling of the riser item already with the fix pass; worth deciding in the same edit.
9. **Worth a look · the salamander frame does not show what it proves.** `caves-descent-salamander-04-02.png`: the channel wall is unlit near-black rock, so a pale animal on a featureless plane still reads as floating — the exact complaint T-62 exists to answer ("are they supposed to float?"). The builder's own read says so. `LESSONS.md` §0 rule 5: a station is a composition, looked through before it is saved; no second framing (a lamp-lit stretch of wall, a lower angle, a tread in shot) was tried.
10. **Worth a look · a goblin body overlaps the hero and nothing measures it.** `meadow-golden-hit-04-02.png`: the third goblin (upper right) overlaps Isabella's head and shoulder in screen space. The probe measures the **club head** against `bones.head` (0.619 m) and goblin-to-goblin separation (1.005 m); `enemies.md` §2.4's push apart is enemy-to-enemy only, and no check exists for a goblin's body against the hero. T-63's complaint was the club through her head; a torso through her shoulder is the same family.
11. **Worth a look · the colour-reservation table is not in the report, and the totem's new hexes are outside it.** `reel-fixes-meadow-04.md` check 3 and the rule sheet ("Any *new* hex — war-paint, belt rope, ear ring, **totem wraps** — is checked … quote the table"). The table exists only as a source comment, `sandbox/meadow-golden/goblins.ts` lines 36–43, and covers the eight body hexes; the war-totem's `GT.shard` and `GT.coal` in `sandbox/meadow-golden/props.ts` line 171 are not in it. Both pass §2.1.3 on my own arithmetic; the row is that nothing in the delivery records them.
12. **Worth a look · `PHASE_0.75_ANIMALS_BRAINSTORM.md` §3 gives the Crystal Caves four salamanders; the demo ships two.** Disclosed as caves deferral (e) and carried from the rule sheet, but it is a live shortfall against the ledger, not a closed one.
13. **Worth a look · `onWing()` identifies a wing rider by float equality.** `sandbox/flight-golden/flight.ts`: `SEATS[v][i]![1] === HIP_WING`. `HIP_WELL` (1.02) and `HIP_WING` (0.855) happen to differ, so it works; a fourth variant that seats a kid at either derived height would silently switch the whole grip solve. P2 (fuzzy / near-duplicate logic); cheap to make explicit.

## 2. Seams between the three builders

- **Two builders touched `_shared/` and their edits are disjoint.** Caves: `rig.ts` only, the ring block (`ringGeo` / `aLift` / `updateRingLift`, about lines 222–275, plus the call at ~338 and the `*.rig` probe payload). Flight: `plane.ts`, whole file. No third builder entered `_shared/`; the per-commit file lists confirm the meadow stayed inside `meadow-golden/`. The merged tree is green (`npm run check` re-run here: tsc, eslint, 44/44).
- **`rig.ts`'s ring at `+0.04`** (the round-1 audit item) is unchanged context in this diff — it had already been lifted before the round; the caves builder added `aLift` on top of it, applied to world y **after** the curve term, on the shared `WORLD_U` objects by reference (T-41 honoured, verified in the shader source).
- **The caves shot its gate and diffed it.** Nine `<scene>-ring-04-01.png` against `<scene>-round1-03-02.png`, every difference attributed: bog-night byte-identical (the flat-ground control), desert-noon 0.30 %, rim-dawn 0.24 %, shadow-wrong 0.39 % (the conforming ring), caves-descent 59.5 % (its own scene), forest-dusk 28.8 % (wall clock, no `ssStep`), frozen-night 8.4 % (the orchestrator's committed T-59 roof, which the baseline predates).
- **Yes — the caves' gate frames show the other two builders' in-flight state, and it is declared.** `flight-golden-ring-04-01.png` 11.1 %, bbox the plane (the well and the pennant); `meadow-golden-ring-04-01.png` 1.4 %, bbox the goblin camp (the new goblins and war-totems). Both are named in the caves report as "the two other builders' in-flight scenes", with `git status` cited. That is the right handling; it also means neither of those two frames is a clean ring baseline.
- **The flight shot its gate but did not diff it** (§1 item 7). Its nine `<scene>-plane-04-01.png` are byte-identical to the caves' nine for 8 of 9 scenes, and two of them are literally the caves' and the meadow's own study frames. Both sweeps were therefore rendered from the *fully merged* tree — fine for catching a regression, useless for attributing one to a single builder.
- **The meadow shot no sweep, correctly** (scene-local change; its rule sheet says so explicitly).
- **One instrument is missing from the tree:** `scripts/probes/plane-04-frames.cjs`, untracked (§1 item 3).
- **No unattributed cross-contamination found.** Every scene's difference from round 1 is accounted for by exactly one of: the ring conform, the plane, the goblins, the T-59 roof, or the wall clock.

## 3. Every deferral the three reports made

**The caves** (`LOG.md` "Reel fixes round 2 — the caves", Deferred / noted):

- (a) The ring's rim at r = 0.62 ramps across a step instead of following it — up to **0.2533 m** on 8 of 20 stands; the fix is a middle radial ring (147 verts, about 0.17 ms) or a shader that samples the ground. *(already with the fix pass)*
- (b) The `J` re-ground is a teleport, against Tier-0 rule 3; argued as a debug comparison key outside that rule.
- (c) `aLift` steps at a column edge on purpose; refreshed only above 0.02 m of movement, so a standing kid's ring breathes against a lift sampled 0.037 m of xz ago.
- (d) T-41's residual: `sandbox/forest-dusk/liam.ts` builds the Forest's own ring, which gets no `aLift` and does not conform. Outside this task's files.
- (e) Two salamanders where `PHASE_0.75_ANIMALS_BRAINSTORM.md` §3 says four.
- (f) The wall-side lock (a mid-climb flip left the belly 1.54 m off the rock); the second animal starts at arc 6 of the second stair so it has wall on both sides.
- (g) Browser and node column histograms differ by a few cells (`[13,62,268,108,40,4]` vs `[14,68,264,107,40,2]`) — `Math.sin`/`cos` last-ulp; both inside the 35–65 % band.
- (h) `makeRock()` split out of `makeCave()` with its own seed, so the Depths floor's per-face colour jitter differs from round 1's.
- (i) The stair still floats over the void past the tier's edge — no piers (round-1 deferral (d), untouched).
- (j) The three `LESSONS.md` rows filed in the **Misc** table with their real section named inside the row, to keep three parallel appends at the file's end.

**The meadow** (Deferred / noted):

- (a) The Ground Pound's own dust in `meadow-golden/main.ts` still uses `_shared/particles.ts`'s `pointsMat`, which does not carry the curved world's bend (T-41) — 0.24 m of float at 20 m from the curve centre. Left for the shared pass.
- (b) The three goblins never share a windup frame in a 22 s fight — `atkT` working; the fan is quoted over every frame instead.
- (c) The club dust `#C9B08A` sits 9° / 13 % from Noah; kept because §2.1.3 governs an enemy's **body** colour and this is the meadow's existing Ground Pound dust, matched deliberately.
- (d) `scripts/probes/meadow-arm.cjs` kept as the measurement that found the two IK defects.

**The flight** (Deferred / noted):

- (a) The brief's ±0.34 m well rows cannot hold two kids abreast (fuselage 0.58–0.68 m, hip block 0.28–0.38 m); the rows are at **±0.16 m** in a 0.90 m well.
- (b) The rim cuts the four at the **shoulders**, not the waist (T-28): deck-to-top-wing is 1.205 m against a 1.43 m seated Liam.
- (c) The demo has never drawn `The Green Meanie` lettering or the north-side cockpit step, so neither survives nor is lost — reported, not invented.
- (d) The seat study framings use 2–7° of pitch at 4.5–8 m rather than the brief's 6 m / pitch 12 (at 12° the eye is above the 2.4 m top wing), and are shot on the level final rather than at 18 s.
- (e) `flight-golden-plane-04-01` (the sweep's own flight frame) is the chase at 2 s, not a plane read.

**Raised by the orchestrator during the round, listed for completeness:** T-67 (Ed's scarf at rest reads as a red pipe), `PHASE_0.75_TWEAKS.md` line 77.

## 4. Checked and clear

§0 rules 1–5, 7, 9, 11, 12 · §0 rule 8 (never-run) for the `J` relief cycle including all three `?relief=` cold loads, the salamanders' `still`→`climb`, the ring's per-frame `aLift`, the goblin state machine (every state, all three, 560 frames), the `N` seat cycle a→b→c→a and all three `?seats=` cold loads via `flight-frames-04.cjs` · §0 rule 10 for the caves (see §2) · T-08 · T-09 · T-10 · T-16 · T-19 · T-20 · T-21 · T-22 · T-24 · T-26 · T-27 · T-29 · T-30 (S1 and S4 notes re-derived from the renders) · T-31 · T-35 (no hit-stop added; `main.ts` line 150 shake and ring flash untouched) · T-39 · T-41 · T-42 · T-43 · T-44 · T-47 · T-48 · T-55 · T-56 · T-57 · T-58 · T-59 · T-60 · T-61a · T-62 · T-63 · T-64 · T-65 (hang, continuity, tip aft, parked sway) · T-66 (bone-in-solid ≥ 0 for the eight bone points, palm-to-grip ≤ 0.015 m, Isabella on −z in all three) · LESSONS Camera rows 1–6 · Rigs rows 1, 3, 5, 6, 8, 9, 10, 11, 12 · Light rows 1–8 · Density rows 1–5 · Process rows 1–8 · Geometry row 1 · Misc rows 1–4 · `enemies.md` §2.1 / §2.1.3 / §2.2 / §2.3 / §2.4 / §2.5 · `bosses.md` §2.6.2 · `npcs.md` §2.2.1 (Ed's socket, Isabella's side) · `dungeons.md` §2.7 (lamps cd 0, count 15, scene under twenty lights) · `PRE_BUILD_TODO.md` (exactly one row added, id **P-10**, lane and status correct; P-05 left open) · Working Rule 7 (trim named; 39 → 44, caves +1, goblins +2, flight +2, each within its cap) · `npm run check` green, 44/44, re-run here.
