# Rule sheet — the shared rigs and the walk, reel fixes round 1 (`docs/qa/briefs/reel-fixes-shared-03.md`)

Written by `rules-librarian`, 2026-09-08. Tier-1 rows fetched per `DEMO_PROGRAM.md` §1. Task rows: **T-41, T-42, T-44, T-45**.

## 1. Applies

**Never-run rule first.** `LESSONS.md` §0 rule 8: "A mechanic is not built until it has been seen to run in a stepped probe, every state reached." All four fixes are mechanics (the bent ring, the solved staff, the three hammer carries, the sprint lock): idle, walk, run, twirl, whirl, sprint-set and sprint-cleared each need a frame or a number, not code that reads right. `LESSONS.md` Process row 5 is the precedent ("a keyed mechanic judged by eye, once, on a stalled clock").

- **Always · `LESSONS.md` §0 rule 9 + Process row 5** — "The clock stalls on a hidden page: step the runtime, never wait for a screen." Every check in §4 of the brief runs under `?step=1` with `ssStep`/`ssSnap`/`ssKey`. **Consequence for T-44:** the double-tap window (0.3 s) and the idle-clear window (0.4 s) must be measured on the clock `ssStep` advances (`scene.ts`'s `simT`/`dt`, T-35), not `performance.now()`, or check 4 (`ssKey('Shift')`, `ssStep(6)`, `ssKey('Shift')`) can never latch.
- **Always · `LESSONS.md` §0 rule 3 + T-06** — "Nothing pops: every state change eases over ≥ 0.15 s." The sprint's 4.5 → 5.4 keeps the existing 8/s ease; the hammer's walk → shoulder carry and the staff's idle ↔ walk blend ease on the rig's `blend`, they do not snap.
- **Always · `LESSONS.md` §0 rule 10 + Process row 3** — a change under `sandbox/_shared/` is checked with **one frame per scene**. Every file in this task except `forest-dusk/walk.ts` and `index.html` is shared; that is brief check 5 (nine scenes, `-shared-03`).
- **Always · `LESSONS.md` §0 rules 11–12 + Process rows 1–2** — `?t=` explicit on every save, frame names lowercase; wait 10–12 s after navigation (a blank save is 58,885 bytes); patch TypeScript with a `.cjs` file, never an inline `node -e`.
- **T-41 (the row itself) + T-27 + Brief §5.1** — "anything that sits in the world bends with it"; and the curve centre is a *camera* property re-centred each frame, the shader bending only by `uCurve · d²` from the centre. This is fix 1 exactly: the ring's `ShaderMaterial` and the ribbon's `MeshBasicMaterial` take the shared `WORLD_U` uniform objects, never copied values, or the ring drifts again the moment the camera travels.
- **`heroes.md` §2.7.5 (canon)** — the ring is "a ground decal under the active hero", outer diameter 1.6 m, additive, `depthWrite false`, "offset 0.01 m with `polygonOffset` so it follows slopes without z-fighting", breathing ±6 % at 1.2 s, and at night driving one pooled point light. The bend must not cost the breathe, the additive blend or the night light (check 1 uses a cave scene with the ring light on).
- **T-56 (`heroes.md` §2.7.5, `dungeons.md` §2.7)** — a ground decal sits at ground + 0.04 with a polygon offset and is checked on *stepped* ground. Same ring, same file; status `observed` and owned by `reel-fixes-caves-03`, but do not regress its offset while editing the ring's shader.
- **T-42 + `heroes.md` §2.3.2 "Prop, idle" (canon)** — "Staff planted 0.20 m ahead of the right foot, both hands on it at chest height, orb pulsing." Fix 2's idle pose is this line verbatim; the 0.20 m is not yours to retune.
- **`heroes.md` §2.4.4 walk (canon)** — "the staff is carried upright, orb forward, **not swung**"; the pigtails lag 0.15 s and the robe hem swings. That is why the right arm loses its swing and the left keeps it.
- **`heroes.md` §2.4.4 Victory (canon)** — "Staff twirl, plant, amber sparkle burst (10 motes), one hand on the hip." The flourish's twirl must still read after the solve; check 2's filmstrip is that guarantee.
- **`LESSONS.md` Rigs row 2** — "a planted or resting prop is parented to the root (or the ground) and the hands are posed to it; a carried prop is parented to the hand." T-42 keeps the staff on the root and poses the hands; T-45 moves the hammer *off* the pure hand-parent so its head can meet the ground. Both are this row.
- **`LESSONS.md` Rigs row 1** — "ease a tracked number, then **set** the bone from it; hooks add on top of the set value, never on top of an eased bone." Suppressing Collette's right-arm swing and posing Isabella's arms must set from a tracked blend, not subtract an offset each frame (the Noah 118°-head bug).
- **T-45 + `heroes.md` §2.4.5 walk/run (canon)** — "The hammer drags behind her at walk (a 0.4 m dust trail, 4 %/step) and comes up onto her shoulder at run." The drag and the shoulder are canon; the dust trail is canon too, so if it is deferred say so in the report and the LOG (do not drop it silently).
- **`heroes.md` §2.3.2 "Prop, idle" Isabella + §2.0 silhouette (canon)** — "Hammer head on the ground at her right, both hands on the shaft top, chin nearly resting on her hands"; §2.0's read-at-distance is "A block on the ground". The idle band [ground − 0.02, ground + 0.05] serves this line.
- **`heroes.md` §2.7.4 walk clip** — the walk loop is "≤ 2.0 m/s"; that is the authority for the brief's walk/run split at speed 2.0, and for the blend easing "toward 1 at 5/s when speed > 0.5 m/s".
- **T-44 + `heroes.md` §2.0 speed table (canon)** — run speeds are per kid: Liam 4.5, Noah 4.375, Collette 4.1, Isabella 4.0. T-44 says the lock is "**1.2 ×** the kid's run speed … Every scene, every kid"; the brief's flat 4.5 × 1.2 = 5.4 is Liam's number only. Build the multiplier off the active kid's run speed and note it, or log the flat value as a demo simplification in `DECISIONS.md`.
- **`heroes.md` §2.4.5 whirl + T-36** — the flourish keeps "arms and hammer out" and its ribbon, and the ribbon's radius **eases in over ~0.3 s** and sweeps ~220°. Giving the ribbon the curve bend must not undo T-36's ease.
- **T-28** — carried props are **stowed** in flight (Collette's 1.7 m staff stood through the top wing). A solved staff must still be stowable; do not make the flight scene's stow depend on the old float.
- **`LESSONS.md` Camera row 1 (`walk.ts` `engaged`)** — the follow camera engages on the first movement input, never on load, so a station is reproducible. Editing `_shared/walk.ts` for the sprint must not engage the camera on a bare Shift.
- **T-30** — a note or a HUD line is derived from the render, not written beside the numbers; the HUD's `⇧ run · ⇧⇧ sprint` is verified by grep *and* by a frame, in three places (`scene.ts` keys, `drawOverlay` hints, `index.html`).
- **`PRE_BUILD_TODO.md` P-04/P-07** — the demo-candidates set and the 0.75 application step wait on Andrew's reel pass; anything deferred here is written down, not left implicit.

**Open observations (not rules; status `observed`, `DEMO_PROGRAM.md` §1 rule 4):** T-40 (the whirl ribbon's gold head vs the bible's ruby) — a design-lead call at the application step; touch the ribbon's shader, not its colour. T-47 (stations walkable), T-43 (rocks), T-55/T-34 (the caves' stair) — other builders' rounds this session.

## 2. Checks implied

1. **Ring bend (T-41, T-27):** brief check 1 on `bog-night` at ≥ 30 m and `caves-descent` at 20 m — quote the unbent and the bent projected screen y and the ring's measured glow-bbox centre; after the fix the bent one matches within 6 px. Confirm the uniform is *shared* (grep for `WORLD_U` passed by reference, not a spread/clone).
2. **Ring identity kept (§2.7.5):** on the cave frame confirm the ring still breathes (two frames 0.6 s apart, radius differs), is still additive with `depthWrite false`, and the night ring light is still present.
3. **Ribbon (T-41, T-36, T-40):** a whirl filmstrip showing the ribbon bent with the world at distance *and* still easing its radius in over ~0.3 s; colour unchanged.
4. **Staff (T-42, §2.3.2, §2.4.4):** brief check 2's 120 stepped frames — max hand-to-axis distance ≤ 0.06 m every frame, left hand ≤ 0.08 m at idle, foot y ∈ [ground − 0.02, ground + 0.06] at idle and ≥ ground + 0.05 at walk; quote the maxima. Plus: the right arm's swing amplitude is ~0 at walk and the left arm's is unchanged (quote both).
5. **Twirl (§2.4.4 Victory):** `bog-night-staff-twirl-{0,1,2}-03` at +0.3/0.6/0.9 s — the orb sweeps a circle about the hands, pivot at the hands.
6. **Hammer (T-45, §2.4.5, §2.3.2):** brief check 3 — min/max of the head's lowest world vertex y relative to `groundY` per phase: idle ∈ [−0.02, 0.05], walk ∈ [−0.02, 0.12], run ≥ 1.0, quoted, with `frozen-night-hammer-{idle,walk,run}-03`. Confirm the walk → run transition eases (T-06: sample the head height across the crossing, no step > the 0.15 s ease).
7. **Dust trail (§2.4.5 canon):** either a stepped frame showing a puff per step at ~0.4 m behind her, or an explicit deferral line in the report, the LOG and `DECISIONS.md`.
8. **Sprint (T-44, §2.0):** brief check 4's three speeds on `meadow-golden` and `forest-dusk` — ≥ 5.3 m/s locked, 2.0 ± 0.1 after the clear, 4.5 ± 0.1 on a held Shift; quote all six. Add: `e.repeat` ignored (dispatch a repeat keydown and show no latch), and the tap-to-clear path exercised separately from the idle-timeout path. State whether the 1.2 × is per-kid or flat.
9. **Sprint on the stepped clock (Tier-0 rule 9, T-35):** the double-tap must latch when the two `ssKey`s are 6 stepped frames apart with no wall-clock time passing — that is the check that the window reads the sim clock.
10. **Nine scenes (Tier-0 rule 10):** `<scene>-shared-03` at each default station with `?t=` explicit, saved ≥ 12 s after navigation, none 58,885 bytes; one line each naming only the expected differences from `-02`.
11. **Gates:** `npm run check` green, `tsc --noEmit` clean; LOG/DECISIONS/LESSONS appends made with a fresh read immediately before each single append; the diff touches only the files the brief lists (confirm `_shared/material.ts` and `_shared/rock.ts` are untouched).

## 3. Not applicable, considered

- **T-24 / T-39** (the shard's 12 % floor and the far fill) — a Shadow Realm paint-and-light row; this task changes no light, and the `-shared-03` frames must simply not shift luminance.
- **T-26** (facing per axis) — no bearing is re-authored here; it applies only if a solved prop needs a new local axis, in which case check the frame, not the guess.
- **T-08 / T-23 / T-17** (station pitch, tilt-up beats, the aurora) — no station or camera is edited; the ring check reads existing stations only.
- **T-09 / T-22** (scatter density and litter colour) — no flora in the files you may touch; `_shared/rock.ts` is another builder's this round.
- **T-37 / T-46 / T-48** (CS-04's length, Isabella's basic-attack fork, the flight look-around) — `story-dependent` or another brief's; the whirl here keeps the bible's existing pose.
