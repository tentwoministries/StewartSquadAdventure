# Rule sheet — The Hearth, reel fixes round 1 (`docs/qa/briefs/reel-fixes-frozen-03.md`)

Written by `rules-librarian`, 2026-09-08. Tier-1 rows fetched per `DEMO_PROGRAM.md` §1. Rows **T-51, T-52, T-53, T-54** carry status `observed (2026-09-08) → fix pass, reel fixes round 1`; they are this task's assignment, so they are listed as rules here and become ledger rows once fixed (`DEMO_PROGRAM.md` §1 rule 4).
**Gap (Working Rule 1):** `world-events-weather.md` §2.7.1 has **no herd-crossing behaviour** (its only elk line is "elk (5: one herd) … the Frozen migration herd", flee radius 9 m) and **no penguin or seal row at all** — both are ★ new in `PHASE_0.75_ANIMALS_BRAINSTORM.md` §3 Frozen, adopted only through T-14, which is still `proposed`. Build the demo behaviour from the brainstorm rows and T-51/T-52/T-54, and say in the report that the bible owes the section (the 20 s / 90 s schedule is a demo number, not canon).

## 1. Applies

**Never-run rule first.** `LESSONS.md` §0 row 8: "A mechanic is not built until it has been seen to run in a stepped probe, every state reached." All four fixes are mechanics (the slide, the roll and the crawl, the pool creature, the crossing) — none is done because the code reads right. §0 row 9: "The clock stalls on a hidden page: step the runtime, never wait for a screen" — every check in §5 of the brief runs under `?step=1` with `ssStep`.

- **T-51** (`PHASE_0.75_TWEAKS.md`) — "a slope is asserted by sampling the terrain along its path (monotonic), and a creature's pitch and roll are local (Euler `YXZ` or a heading parent)". Governs `terrain.ts` `DRIFT` and the penguins' slide pose in `creatures.ts`.
- **T-52** — "a roll is about the body's own long axis (a lazy half-roll onto the back and back), and a moving seal hump-crawls (a 3 Hz body pulse, the nose lifting) facing its velocity". Governs the seals in `creatures.ts`.
- **T-53** — "**a sheet of water lies in a basin** — the terrain under it below the sheet and the shore around it at or above it — asserted by sampling; and no creature's home is inside a pool". Governs the ice-fall pool block in `props.ts` plus one creature home in `creatures.ts`.
- **T-54** — "a scripted pass a station is composed for fires on its own inside the first half-minute (the first crossing at ≈ 20 s, the second at ≈ 90 s, key 0 still forces one)". Governs the herd schedule.
- **T-38** (T-53's named parent) — "where a creature or a kid is placed at water, the bible names a shore with at least 3–4 m of walkable ground behind it". The pool's new shore must be standable, not a lip; the moved creature's new home sits on it.
- **`LESSONS.md` §0 row 4 / T-26 / STUDY_NOTES §6 rule 3** — "a mesh built along `+x` (the plane, animals) takes `90° − bearing`"; the brief's seal convention is exactly this. `LESSONS.md` Rigs row 7: "wrap every accumulating angle to [−π, π]" — the roll's `u` and the crawl heading.
- **`LESSONS.md` Rigs row 3** (Collette's orb / Isabella's hammer) and row 6 (the hut roof: "rotate a part about its own centre, then translate") — the same class as the slide pose: a local transform, not a world-axis one.
- **`LESSONS.md` Density and scale row 4** — "anything placed near the plate's edge asserts `inside()`; a bank is found by marching, not by subtracting a radius". The drift's new foot and the pool's shore are found by sampling `terrainY`, never by arithmetic on a radius.
- **T-06 + `MOTION_TEMPO_NOTES.md` / `LESSONS.md` §0 row 3 / STUDY_NOTES §6 rule 4** — "every state change eases over ≥ 0.15 s, every recurring motion runs on two incommensurate rates". The 0.8 s / 1.2 s / 0.8 s seal roll, the waddle↔slide transition and the herd's departure all ease; the 3 Hz crawl pulse rides a second, incommensurate rate.
- **`PHASE_0.75_ANIMALS_BRAINSTORM.md` §3 Frozen (via T-14)** — penguin colony (6): "waddle, belly-slide down the same drift, waddle back up", reaction "one slides to a hero's feet and looks up"; seal (2): "lies on the ice; rolls over; slaps once", reaction "slides into the water at 5 m" (the brief's 5 m flee is this row); elk wonder: "crosses the frozen lake under the aurora once per game night" — T-54's schedule is that wonder made to fire.
- **`world-events-weather.md` §2.7.1 shared rules** — ≤ 400 triangles and ≤ 3 clips per animal (T-14 adds one signature clip), flee 5 m/s for 4 s then resume idle, "a fleeing animal never runs off a rim (a rim repulsion at 3 m)". The fleeing seal must not end in the void or inside the lake wall.
- **`LESSONS.md` Camera row 4 + T-30** — "after placing a landmark, look through every station that faces it; keep an exclusion radius round station subjects"; a station's note is derived from the render, not written beside the numbers. Moving the drift and sinking the pool changes `PG`, `S4` and `S2`: read the frames, and any proposed station numbers come from a rendered frame.
- **`LESSONS.md` §0 rows 10–12 + Process rows 1–3** — `_shared/` is off-limits (a change there needs one frame per scene, five); scene-local edits need only this scene's frames; patch TypeScript with a `.cjs` script, never inline backticks; `?t=night` explicit on every save, names lowercase with the `-03` suffix; wait 10–12 s after navigation (a blank save is 58,885 bytes).
- **STUDY_NOTES §6 rules 7 and 10** — terrain is a 0.5 m grid with per-face jitter and "keep the water a wall for walking (ground below −0.25 m)"; and "don't build the game" — the herd schedule is scene state, not a sim.
- **`PRE_BUILD_TODO.md` P-07** — the 0.75 application step waits on "Andrew's pass on the reel", so anything deferred here is written into the report, not left implicit.

**Open observations (not rules; status `observed`, `DEMO_PROGRAM.md` §1 rule 4):** T-41 (ring and curve) and T-47 (station walkability) are other builders' rounds this session — do not fix them here, but if the moved pool or drift makes a station unwalkable, report it to the T-47 task. T-43 (rocks displaced per index tear) covers `frozen-night/flora.ts` `boulderGeo`, which the rocks builder owns this round: leave the 10 snow-hump rim boulders' geometry alone and only keep them on the shore. T-34/T-55/T-56 are caves rows, no constraint here.

## 2. Checks implied

1. **Slope by sampling** (T-51, Density row 4): quote the 20-point `groundY` profile top → bottom along the slide path — strictly non-increasing, total drop ≥ 1.5 m; also quote `lakeD` at the drift's new top (> 1.35) and confirm the foot meets the shore.
2. **Local pitch** (T-51, Rigs row 3, T-26): mid-slide, the belly's world vector y ≤ −0.85 and the head vector's horizontal component within 20° of the slide direction; the heading is unchanged by the lie-down.
3. **Stepped probe of every state** (never-run rule): one 22 s penguin loop under `?step=1` (`ssStep(1320)`, every 10 frames) showing waddle-up (y non-decreasing, tol 0.02) and slide-down (y non-increasing) both reached; frames `frozen-night-pg-03` and `frozen-night-slide-03`.
4. **Seal roll about the long axis** (T-52): over one roll the long axis's world direction changes < 10°, the roll reaches π ± 0.1 and returns to 0 ± 0.05; the 0.8/1.2/0.8 s envelope is eased, not stepped (T-06).
5. **Seal flee** (T-52, T-26, §2.7.1): Isabella within 5 m → ≥ 3 m in 3 s, heading within 15° of velocity under the `90° − bearing` convention, `scale.x` oscillating in [0.9, 1.1]; confirm the flee path does not leave the plate or enter the water wall. Frame `frozen-night-seals-03`.
6. **Pool basin** (T-53): 72 bearings at r 7.3 and 8.0 → min `terrainY − poolY` ≥ −0.02; 100 points at r < 6.5 → max `terrainY − poolY` ≤ −0.05. Quote both numbers. Frames `frozen-night-s4-03` and `frozen-night-pool-approach-03` (pitch 10, d 12): no sheet edge or underside.
7. **Shore is standable** (T-38): state the width of walkable ground behind the new shore where the moved creature's home lands (target 3–4 m), and name the creature, its old home, its new home and the frame it is visible in.
8. **Herd fires on its own** (T-54, never-run rule): no key pressed, sample `herd.state` and the bull every 60 frames to 120 s — `walk` begins 18–24 s, bull ≥ 15 m from the park point at 45 s, second `walk` 85–95 s; then a fresh load with `ssKey('0')` at 5 s starts a `walk` within 1 s. Frame `frozen-night-s2-03` at 32 s; if the route misses S2's frame, say so and propose the route or station change.
9. **Stations re-read after the moves** (Camera row 4, T-30): confirm `PG` still frames the drift and `S4` the pool from the saved frames; any proposed station numbers quoted from a render, not from arithmetic.
10. **Process** (§0 rows 10–12, Process rows): every frame lowercase, `-03`, `?t=night` explicit, saved ≥ 12 s after navigation, none 58,885 bytes; the diff touches only `frozen-night/{creatures,terrain,props}.ts` (+ at most one line of `main.ts`), never `_shared/` or `flora.ts`; edits made with a `.cjs` script; `npm run check` green; `LOG.md`, `DECISIONS.md`, `LESSONS.md` appended once each, re-read first.

## 3. Not applicable, considered

- **T-17 / T-23** (aurora altitude, a tilt-up beat) — `L1` is already the pitch-5° aurora station and this pass does not move it; `frozen-night-l1-03` is a record frame only.
- **T-24 / T-39 / T-20** (the shard's floor, the key-hemi pair) — Shadow Realm lighting rows; this pass changes no light and must not retune the Frozen night column.
- **T-44 / T-48** (sprint lock, CS-04 look-around) — built in other scenes' rounds; no walk or camera code is in this task's file list.
- **T-09 / T-22** (scatter density and litter colour) — `flora.ts` is the rocks builder's this round; do not thin or recolour scatter to make the drift read.
- **T-49** (the Bog's frogs on real pads) — the same class of bug as T-53's hidden hopper, but its rule is Bog-specific; here the constraint is only "no creature's home is inside a pool".
