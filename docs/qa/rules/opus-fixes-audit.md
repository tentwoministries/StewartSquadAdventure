# Rules audit — the Opus fixes (P-02), `d09be90..HEAD` on `phase-0.75-visual-studies`

`rules-auditor`, 2026-09-08. Read: `LESSONS.md` §0 → Misc (whole file), `PHASE_0.75_TWEAKS.md` (T-01 … T-39), `PRE_BUILD_TODO.md`, the five briefs and five rule sheets under `docs/qa/`, `OPUS_FIX_PLAN.md` §4–§7, the diff of `sandbox/` and `scripts/` (8 commits, 116 files), the LOG sections for all five tasks, and the frames `shadow-wrong-s1-02-01`, `shadow-wrong-swing-0-02-01`, `meadow-golden-s1-02-03`, `rim-dawn-s2-02-01`, `flight-golden-wg-02`. Judgment of taste: none. Nothing edited.

## 1. Suspected misses

1. **Shadow · brief check 2 / rule sheet check 3 (T-24's 12 % floor at the swing station)** — `docs/design/mockups/LOG.md:103` (iter 8) reports only the seat's displacement (1.334 m, 255 px); the luminance measurement the check demands for *both* swing frames is never quoted (only S1 and S3 are measured, iter 12). In `shadow-wrong-swing-0-02-01.png` the near-black wedge over the right third (about x 950–1600, y 60–620) looks well past "a tenth of the frame". **Sure** the check was not run; **likely** it would fail.
2. **Shadow · Tier-0 rule 5 / `LESSONS.md` Camera row 4 (line of sight, exclusion radius round the station's subject)** — the new `SW` station (`sandbox/shadow-wrong/main.ts`, yaw 237 pitch 14 d 7): in `shadow-wrong-swing-0-02-01.png` an unlit canopy slab fills the right third of the frame and crowds the swing, which is the station's subject; the swing seat and ropes read against it. The station was moved to escape one occluder (iter 8) and picked up another. **Likely.**
3. **Shadow · Tier-0 rule 6 / T-09 Density ("within a step of the ground's own colour")** — the thinning half of the row is proved (20,761 → 5,816, LOG iter 11), the colour half is not: `shadow-wrong-swing-0-02-01.png` foreground (e.g. around x 430–1200, y 600–900) carries cyan-blue cone tufts and red-orange flecks over violet ground. **Likely.**
4. **Flight · T-28 / scores §3 change 5 / `LESSONS.md` Misc row 2 (seated kids)** — `LOG.md:130` claims "the cockpit rim cuts the four at the waist and four heads separate". In `flight-golden-wg-02.png` the red kid (about x 950–1060, y 300–500) reads as sitting *on top* of the fuselage with her skirt draped down its outside — the exact shape T-28 was written about. A real seated pose does exist in code (`sandbox/flight-golden/main.ts:124–129`), so this is a placement/height reading, not a missing pose. **Likely.**
5. **Flight · `OPUS_FIX_PLAN.md` §4 (frame naming, "the `-02` suffix")** — the chase frame landed as `docs/design/mockups/flight-golden-ch-03-01.png`, one character from pass 1's `flight-golden-ch-03.png`, and carries no `-02`. The implementer names the cause (the shot plugin's own counter, `LOG.md:131`), but the plan line is not satisfied and the two passes' frames are now near-indistinguishable in the directory. **Sure.**
6. **Ledger · `LESSONS.md:5` ("add a row whenever a demo fixes something a kid could have noticed")** — §6's eight rows are all present, but two defects *found and fixed inside this pass* have no ledger row: the songbird burst that never fired because Liam stood at exactly 5.00 m against a `< 5` trigger (`sandbox/rim-dawn/life.ts`, LOG:194) and the hit-stop that zeroed `dt` while handing on the wall clock, so breath, blink and the leg swing ran through the stop (`sandbox/_shared/scene.ts`, LOG:165). The second is only a bible tweak (T-35), not a rule the next implementer's sheet can fetch. **Likely.**
7. **Shadow · T-24 floor, still unmet** — `shadow-wrong-s1-02-01` 59 % and `shadow-wrong-s3-02-01` 66 % of pixels under 12 % (LOG:104, :107). Honestly reported and T-39 proposes the reword ("nothing renders below 12 % *that a surface colour can carry*"), but the row as written is a rule and no one has accepted the reword; until Fable rules on T-39 the delivery is short of it. **Sure** (known deferral; listed because the row is still the standing rule).
8. **Meadow · brief check 1 / rule sheet ("Quote the whole log")** — `LOG.md:158` gives a summary, not the probe log, and the sequence it quotes, "chase → dead → windup → hit → recover", cannot be an ordering (dead precedes windup). The states are believable and the probe exists (`scripts/probes/meadow-goblins.cjs`), but the never-run rule's evidence is a summary sentence with an incoherent ordering in it. **Worth a look.**
9. **Meadow · `heroes.md` §2.5.4 / rule sheet check ("the ring is still 0.3 m tall at 1.9 m and gone by 0.25 s")** — the ribbon's visible life is now ~0.92 s (`sandbox/_shared/kid-isabella.ts`, `ribOut = smoothstep(ribT,0,0.12) * (1 - smoothstep(ribT,0.62,0.92))`) and the scene's *combat* whirl fires the 2.4 s flourish clip (`sandbox/meadow-golden/main.ts:120,129`), so the bible's 0.25 s combat ring exists nowhere in the demo. T-36 proposes the split but records only the sweep. **Worth a look.**
10. **Meadow · T-08 / scores §4 change 3 (a tall thing in the hero frame for scale)** — `meadow-golden-s1-02-03.png` at pitch 40 still contains no tall element; the rule sheet routed this to `L1` (pitch 32 d 26) and neither `meadow-golden-l1-02` nor `-w1-02` was re-shot (`-l1-a-01` / `-w1-a-01` are still the only ones). The §4 re-shoot list does not name L1, so this is a rule the list did not carry. **Worth a look.**
11. **Flight · T-28's own numbers** — `docs/design/PHASE_0.75_TWEAKS.md` T-28 still reads "0.84 m apart across and 0.72 m along", while the delivery seats them 1.10 m along × 0.76 m across (`sandbox/flight-golden/flight.ts:121–132`, and a `DECISIONS.md` line). The row is what the bible inherits; it now cites a demo that no longer matches it. **Worth a look.**
12. **Rim · brief check 4 / rule sheet check 4 ("3 s pause at each end")** — `LOG.md:194` quotes 0.9 m/s and "**3.00 s** pause at u = 1"; the other end of the route is not quoted. **Worth a look.**
13. **Shadow · Tier-0 rule 8 (never-run) for S4's climbing water** — the 46 drops that rise the block face and carry over the lip (`LOG.md:105`) are shown only in one still (`shadow-wrong-s4-02-01`); no stepped pair or probe shows the upward motion, which is the whole point of the beat. **Worth a look.**
14. **Flight · brief check 2 ("no discontinuity larger than 2 cm between frames")** — not met as written (max per-frame 0.08682 m); re-read as a second-difference test (0.01519 m) and logged in `DECISIONS.md`. Reasoned, but it is a stated check turned into a different check by the implementer. **Worth a look.**
15. **`docs/design/PRE_BUILD_TODO.md:9`** — P-02 ("The Opus fixes") is still `open` after the pass delivered; the table's own rule is that an item gets a `DECISIONS.md` line and a status when done. **Worth a look** (likely Fable's to close at the gate).

## 2. Checked and clear

Tier-0 §0 rules 1, 2, 3, 4, 7 (point-light counts: shadow 6 constructors, meadow 1, rim 0, flight 0 — all far under twenty), 9, 10 (nine `-runtime-02` frames + seven `-simt-` frames + four `meadow-check-` frames for the two `_shared/` changes), 11 (`?t=` explicit, names lowercase — flight's counter aside, item 5), 12 (`scripts/probes/*.cjs`, no inline `node -e`).
`LESSONS.md` Camera rows 1, 2, 3, 6 (arc-length sampling); Rigs rows 1, 3, 5 (the goblin brake), 6 (the look hook); Light rows 6, 9 (waterfall translucency); Density rows 1, 3, 4 (`inside()` marching); Process rows 1 (no 58,885-byte save in the set), 2, 3, 4, 5; Misc row 1 (rings hidden in the cutscene).
T-06, T-09 (meadow, measured: warm flecks 1.14 → 0.01 per mille), T-10, T-16, T-21 (W1's own pair), T-22, T-25, T-26, T-27, T-29, T-30, T-31, T-32, T-33, T-38.
`OPUS_FIX_PLAN.md` §4 (all nine + three named frames exist), §5 (flight tests landed; the rest are with the tests agent — not counted), §6 (§0, Misc and all eight rows present), §7 first bullet (hub tabs and `data-cat`).
`PRE_BUILD_TODO.md` P-05/T-34 correctly weighed and left alone by all five tasks.
Working Rule 2 re-verification quoted for `getPointAt` / `getTangentAt` / `getLength` / `CatmullRomCurve3` against `node_modules/three` r185 (`LOG.md:122`).
Deferrals weighed and accepted as honest: the plane's tail skid (`_shared/plane.ts:30`, outside the task's files, identified by raycast), the pond's dark mirror at rim S3, the bobber's ripple centre (no such uniform on the Forest's ribbon mesh), the seat pose owed in `_shared/rig.ts`, T-37's 28.5 s against §2.10's 14–18 s (row raised, `story-dependent`). The pure goblin state-machine test is with the tests agent and is not counted here.

## 3. None

Not applicable — section 1 is not empty.
