# Lessons from the sandbox: the defects we fixed, so the game never re-makes them

A review list for Phase 1 and after (Andrew, 2026-09-07: "make sure that we don't take defects that we fixed in the demos when you go implement the full game"). Not rules for the imagination; a checklist of things that went wrong in the demo scenes, what caused them, the rule that came out, and where the working version lives. The inspection checklist's P0 step points here: before building a system this file names, read its rows.

One row per defect: **symptom → cause → the rule → where it lives now.** Add a row whenever a demo fixes something a kid could have noticed.

## 0. Always (the list every task carries)

The Tier-0 invariants (`DEMO_PROGRAM.md` §1): pasted verbatim into every implementer prompt, on top of the Working Rules.

1. The anti-palette never ships (uniform mid-green, gray fog on gray ground, pastel everything, pure-black shadows, default Three.js lighting, neon UI over a soft world).
2. One clean key light and the hemisphere as fill.
3. Nothing pops: every state change eases over ≥ 0.15 s, every recurring motion runs on two incommensurate rates.
4. The facing conventions, stated per axis: a kid faces bearing b with `rotation.y = 180° − b`, a +x-built creature with `90° − b`.
5. A station is a composition: look through its lens before saving it, and keep its line of sight clear.
6. Scatter stays within a step of the ground's own colour and is sparse and clustered; density comes from about twenty hand-placed props per frame.
7. Large emissive faces at ≤ 0.5 gain; only small quads at 1.6+; about twenty point lights a scene.
8. A mechanic is not built until it has been seen to run in a stepped probe, every state reached.
9. The clock stalls on a hidden page: step the runtime, never wait for a screen.
10. A change under `sandbox/_shared/` is checked with one frame per scene; a scene-local change needs only its own.
11. `?t=` explicit on every save; frame names lowercase.
12. Patch TypeScript with a `.cjs` script file, never an inline string with backticks.

## Camera

| Symptom | Cause | The rule | Lives now |
|---|---|---|---|
| Every station's frame slid toward the hero before anyone touched a key; a station aimed at a landmark ended up centred on the kid | the follow camera lerped toward the hero every frame from load | a follow camera engages on the first movement input, never on load; a station is a reproducible frame until then | `sandbox/_shared/walk.ts` (`engaged`) |
| Shadows vanished when a kid walked 25 m from the station | the directional light's shadow box was fixed on the station target | the key light's target and its shadow box follow the walked hero (a rounded position if shimmer shows) | `sandbox/_shared/scene.ts` (`key.target` per frame) |
| The aurora never appeared from any gameplay station | at pitch ≥ 18° the frame's top edge is below the horizon; the bible hangs the curtains at 160–200 m | anything hung in the sky needs a camera beat that tilts up, or it is only ever a wash on the ground (T-17) | `sandbox/frozen-night/main.ts` station `L1` at pitch 5°; `aurora.ts` |
| Two cave stations rendered black | the camera sat outside the back-face shell, or inside a tier's rock | a station in an enclosed space is checked against the enclosure's geometry; a back-face shell is invisible from outside (useful on purpose for a cutaway) | `sandbox/caves-descent/main.ts` stations `L1`, `W1` |
| A landmark filled the frame or blocked the station's subject (a mushroom, a cypress, a rock shelf) | props authored without checking the station's line of sight | after placing a landmark, look through every station that faces it; keep an exclusion radius round station subjects (the hut, the snail) | Bog `flora.ts` (`clear()` radius round the hut), Desert `props.ts` |
| A cutscene flight stepped 47 → 89 m/s at a beat boundary and cruised at six times the bible's number | the spline was sampled by parametric `getPoint(u)` with `u` lerped between waypoints, so speed came from waypoint spacing | sample a spline by arc length against a speed schedule; parametric `getPoint` steps at every waypoint | `flight-golden/schedule.ts` (this session) |
| Andrew pressed `4` at Home, Wrong and Collette could not move: four seconds of W, and of S, moved her 0.00 m, and 0 of 36 bearings would take a step | the placement was composed through the lens and never walked from — it sat 3 m inside the mirror stream's cut, at ground −0.62 m against the walk's −0.25 m water wall | a station's kid placement is *probed for walkability* before it ships (W held for a second moves ≥ 1 m) or its note marks it a beat; the walkable bank is marched to, not guessed, and the candidate is judged in the station's own lens (NDC) as well as in metres | `scripts/probes/runtime-stations.cjs` (every scene × station), `shadow-wrong/main.ts` S4 |

## Rigs and animation

| Symptom | Cause | The rule | Lives now |
|---|---|---|---|
| Noah's head pitched 118° backwards mid-laugh; the close-up showed his hair | the base eased `head.rotation.x` toward a target and the kid hook subtracted a laugh offset every frame, so the offset accumulated through the easing | ease a tracked number, then **set** the bone from it; hooks add on top of the set value, never on top of an eased bone | `sandbox/_shared/rig.ts` (`lookYaw`, `lookPitch`) |
| Collette's orb sat at her chest; Isabella's hammer lay flat on the ground | props parented to a hand inherit the arm's axis, so a staff "up" the hand points along the arm | a planted or resting prop is parented to the root (or the ground) and the hands are posed to it; a carried prop is parented to the hand | `kid-collette.ts` (the staff on the root), `kid-isabella.ts` (the hammer leaning from the ground to the hand) |
| A close-up showed the back of a kid twice (Liam in session 1, Noah in session 2) | facing conventions mixed up: a rig's eyes are on local +z, creatures are built along +x | a kid faces bearing b with `rotation.y = 180° − b`; a +x-built creature with `90° − b`; check a close-up against the rig's forward axis, not the camera's guess | `rig.ts` (`kid.face`), `creature.ts` (`rotY`) |
| Noah's lowered hair cap became a helmet that hid his eyes | a hemisphere cap lowered to cover the nape covered the face too | hair is two pieces: a cap that stops above the eye line and a nape piece on the back half only | `kid-noah.ts` (cap + nape) |
| The roof of Neve's hut stood up like two wings | slabs translated first and then rotated about the hut's origin | rotate a part about its own centre, then translate it into place (`applyMatrix4(rotation)` before `translate`) | Frozen `props.ts` (the roof) |
| A creature turned the long way round after a few turns (the deer, session 1) | an accumulating heading was never wrapped | wrap every accumulating angle to [−π, π] each step; take the short way | `creature.ts` |
| Three goblins converged on 1.00 m from their target and never attacked, for sixteen seconds | the skid brake eased speed to zero at the same distance the windup tested for | a brake that eases to zero at the trigger distance never crosses it; brake past the line or trigger before it | `meadow-golden/goblins.ts` |
| Noah never looked up at the towers; three of four kids stared at Liam | the runtime sets every non-active kid's `lookAt` to the active kid before the rig update, so the scene's write is dead | scene code may not write a non-active kid's `lookAt`; use the `look` hook | `sandbox/_shared/scene.ts` (`SceneWorld.look`) |
| The penguins slid down the bank lying on their sides; the seals back-flipped on an axle | `rotation.x` in the default Euler order XYZ is a *world*-axis tilt, so a body that has been yawed rolls about someone else's axis | a lie-down, a pitch or a roll is about the body's own axis: set `rotation.order = 'YXZ'` (or parent the mesh to a heading node) and then `rotation.x` is the side axis and `rotation.z` the long one; check it by reading the *world* direction of the local axes in a probe, not the frame | Frozen `creatures.ts` (the penguins' lie-down, the seals' half-roll) |
| A creature rolling onto its back sank half-way through the ice | the roll turned about the mesh's origin, which sits at the ground, not through the body | roll or pitch a body about *its own centre*: translate the geometry down by the axis height once at build and lift the mesh by the same amount (the hut-roof rule, one axis further) | Frozen `creatures.ts` (`SEAL_AXIS`) |
| A "slope" the colony walked down and slid up | the drift's authored top sat inside the lake basin's smoothing, so `terrainY` pulled the top under the foot | a slope is asserted by sampling the ground along the path it is used for (20 points, monotonic, and the drop quoted) before any motion is written for it — the same rule as "a bank is found by marching" | Frozen `terrain.ts` (`DRIFT`), `scripts/probes/frozen-drift.cjs` |
| A sheet of water floated over its own ground, with a creature hopping in it unseen | the disc was laid at one sampled height over terrain that fell away, and no home was ever tested against it | a sheet of water lies in a basin the terrain makes for it — bed below the sheet inside, shore at or above it outside, both asserted by sampling — and every home and landing spot is cleared out of the footprint by marching | Frozen `terrain.ts` (`POOL`), `creatures.ts` (`poolClear`), `scripts/probes/frozen-pool.cjs` |
| The elk crossing the lake — the beat a station was composed for — only ever ran when someone pressed `0` | the scripted pass was wired to a key and nothing else called it | a pass a station is composed for fires on a schedule of its own; the key stays as a way to force the next one, and the schedule is proved by a stepped probe that presses nothing | Frozen `creatures.ts` (`CROSS_AT`), `scripts/probes/frozen-herd.cjs` |
| The selection ring climbed a kid's legs the further she walked from the station — at the knees at 30 m, the waist at 40 | the ring is a `ShaderMaterial` and the whirl ribbon a `MeshBasicMaterial`, and neither carried the curved world's `y -= uCurve * d²` that the body's `makeWorldMaterial` applies | anything that sits in the world takes the world's bend — rings, ribbons, decals, sprites — driven by the *same* shared uniform objects, passed by reference and never copied, so it follows when the camera re-centres the curve | `rig.ts` (the ring's vertex shader), `kid-isabella.ts` (the ribbon's `onBeforeCompile`), both on `material.ts`'s `WORLD_U` |
| Collette's staff floated 0.24 m off her side and bobbed along beside her; Isabella's hammer head hung 0.19-0.45 m *under* the ground | a prop parented to the root and then posed by eye, or parented to a hand so its far end goes wherever the arm points | a prop that touches the world is **solved** every frame between its contact point and the grip, and the hands are IK'd onto it: the contact point rides the ground under **the prop** (not under her feet, clamped so it cannot fall off a boardwalk's edge) and the box's lowest *corner* is what the ground has to clear; ease the contact points, never the pose | `rig.ts` (`ikArm`, `lowestDrop`, `KidCtx.groundRel`, `Kid.setGround`), `kid-collette.ts` (the staff), `kid-isabella.ts` (the hammer's three carries) |

## Light, fog and materials

| Symptom | Cause | The rule | Lives now |
|---|---|---|---|
| The bible's dusk rendered as deep night (session 1) | three r155+ lights are physical; the bible's intensities were written for a non-physical renderer | key ×3.0, hemisphere ×9.0, point lights in candela (T-07); then tune the whole column by eye on a real frame | `style.ts` `UNITS`, `style-draft.json` |
| The whole Bog vanished into violet past the jetty at the wide station | the bible's Bog fog (15/41 m) is right for the feel and wrong at the study camera | fog ranges are camera-relative and are judged at the wide station as well as the hero station (T-16) | `biomes.ts` Bog rows |
| The Desert's dusk sand went orange-red instead of taking violet shadows | the dusk key too warm and strong for the hemisphere | when a row promises a shadow colour, the hemisphere carries it and the key is cooled to let it (T-19) | `biomes.ts` Desert dusk |
| Low dune troughs were painted as lake bed | a colour rule keyed on height alone | colour rules for water beds test "inside the basin", not "below a height" | Desert `terrain.ts` (`faceColor`) |
| Frame rate dipped and every material recompiled when lanterns were added | each lantern carried a point light | most lanterns are emissive-only; only a scene's few casting lights are lights (cd 0 = no light object); keep a scene under about twenty point lights | `lantern.ts` (cd 0) |
| Per-instance glow could not vary (the caps near Collette, the crystals near a kid) | the emissive attribute is per vertex, shared by all instances | ride the per-instance gain on `instanceColor` (verified: `USE_INSTANCING_COLOR` is a vertex define in three r185); set grey colours for a pure gain | `material.ts` (`vSsGlow *= instanceColor.g`) |
| The waterfall was a solid white pillar | additive ribbon at full alpha with a bright colour | water ribbons are translucent (alpha ≤ 0.35 after edges) and coloured toward the sky, with the mist and spray doing the rest | Caves `props.ts` (`fallMat`) |
| The tent's canvas blew out to white (session 1) | emissive gain 1.6 on a whole face under bloom | large emissive faces at ≤ 0.5; only small quads (windows, glass) at 1.6+ (T-10) | Forest `props.ts` |
| The Rootways' rising wall seam blew to white under bloom | a telegraph authored as a large emissive face at high gain | a telegraph seam is a small emissive; large or bright ones blow to white under bloom (T-10, again) | Forest `props.ts`; the Rootways' wall seam on the experiment branch |

## Density and scale

| Symptom | Cause | The rule | Lives now |
|---|---|---|---|
| Grass, flowers and pebbles read as confetti (session 1) | the bible's per-m² densities at the station camera | density comes from about twenty hand-placed props per frame; scatter is sparse and clustered (T-09) | Forest `scatter.ts`, every scene's flora |
| Red pebbles across the desert read as confetti | a saturated pebble colour on a saturated ground | ground scatter is within a step of the ground's own colour | Desert `flora.ts` |
| At pitch 48° no tree canopy entered the hero frame (session 1) | the bible's station pitch | the hero station sits low enough for the tall things to enter the frame; the tall things are what give scale (T-08) | every scene's `L1` station |
| A deer stood in mid-air over the void in a saved frame | a bank was computed by subtracting the pond's radius, past the plate's organic edge, with no ground check | anything placed near the plate's edge asserts `inside()`; a bank is found by marching, not by subtracting a radius | `rim-dawn/` placement helper (this session) |
| A kid walked *inside* the rock down the caves' first stair until it cleared the Landing, and the stair's top read as geometry pushed into the tier | the tier's columns were placed wherever the tier said yes, including under the stair's band, and topped out at tier height while the stair had already dropped 0.8 m | where a stair crosses a tier, **the tier is cut to the stair**: a column under the band tops out below the *lowest* tread its own body reaches, and the join is checked by raycasting the drawn mesh against the walked `groundY`, not by eye | `caves-descent/terrain.ts` (`cutAt`, the channel's wall and shoulders) |
| The kids' selection rings did not appear on the caves' tiers "even when you are technically on one", and feet sank into or floated over the next rock | a walkable surface's *rendered* tops carried a ±0.125 m jitter its `groundY` knew nothing about; a decal is flat and the rock under it was not | a walkable surface's rendered tops agree with `groundY` to within 0.05 m — a column's top is one number, so its tier is flat — and a ground decal is checked on the scene's *stepped* ground, over a 0.9 m disc round the kid, not only on its flat ground | `caves-descent/terrain.ts` (flat tiers, `r() * 0.03` sink), `tests/unit/sandbox/caves.test.ts` |

## Process

| Symptom | Cause | The rule | Lives now |
|---|---|---|---|
| A saved frame was a blank canvas | the save ran before the first rendered frame | wait for two rendered frames (the save renders one itself); when driving the page from outside, wait 10–12 s after navigation | `scene.ts` `save()`, `shot.ts` |
| A patch silently did nothing, or bash printed "command not found" | template literals and backticks in an inline `node -e` string were eaten by the shell | patch TypeScript with a `.cjs` script file, never an inline string with backticks | this session's scratchpad recipe |
| A change in `_shared/` broke a scene nobody was looking at | shared code, one scene verified | a change to `sandbox/_shared/` is checked with one frame per scene (five); a scene-local change needs only its own | `LOG.md` habit |
| Five scenes shipped with their motion never watched; the sim ran at about 3 % of real time | a hidden pane and a background tab both stall the clock (`visibilityState: hidden`, the fallback tick throttled) | step the runtime with `ssStep`, never wait for a screen; read `ssWorld.hud()`, not the DOM | `sandbox/_shared/step.ts` (being written this session) |
| The goblins never attacked, and the log recorded that the scene "read first time" | a keyed mechanic judged by eye, once, on a stalled clock | a keyed mechanic is not done until a stepped probe has shown every state reached | `scripts/probes/` (this session), `meadow-golden/goblins.ts` |
| A "check green again" commit left `npm run check` red with 2,060 lint errors | the lint fix was verified in a worktree where the gitignored build output had never been generated | a lint fix is verified where the failure showed, with the build output present; `a05ab6b` passed in a worktree that had none | `eslint.config.js` (`dist-demo/**` ignored) |
| The songbirds never fired at load: Liam stood at exactly 5.00 m and the trigger was `< 5` | a strict inequality on a hand-placed distance that landed exactly on the boundary | a trigger on a distance uses `<=` (or the placement sits clearly inside it); a keyed beat is stepped once at load to see it fire | `sandbox/rim-dawn/life.ts` (`BURST_R`, `<=`), `scripts/probes/rim-birds.cjs` |
| The first hit-stop froze positions but breath, blink and the leg swing kept moving through it | the stop zeroed `dt` and still handed the rigs the wall clock `t` | a hit-stop holds the sim clock as well as `dt`; the wall clock drives only `uTime` and the effects the scene marks exempt (T-35) | `sandbox/_shared/scene.ts` (`simT`), `scripts/probes/meadow-hitstop.cjs` |

## Geometry

| Symptom | Cause | The rule | Lives now |
|---|---|---|---|
| Andrew's son: the camp boulders, the stream rocks and the Hearth's boulders were see-through — "the triangles come off and you can see through the unmet seams"; the caves' shell the same | `IcosahedronGeometry` is **non-indexed** in three r185 (detail 1 = 240 vertices, three per face, over 42 unique positions), and every site displaced it *by index* with a fresh random per vertex, so the three copies of a shared corner moved apart and the faces separated | displace a non-indexed geometry per **unique position**, never per index; a rock is checked as a closed surface — every undirected edge shared by exactly two faces — before and after (T-43) | `sandbox/_shared/rock.ts` (`displace`, `openEdges`), `tests/unit/sandbox/rock.test.ts`, `scripts/probes/rocks-closed.cjs` |

## Misc

Rows that fit no section yet; when three or four are about the same thing, the next tidy gives them a heading (`DEMO_PROGRAM.md` §1, rule 3).

| Symptom | Cause | The rule | Lives now |
|---|---|---|---|
| The kids' selection rings were drawn on the fuselage through the whole flight cutscene | world-space gameplay UI left enabled during a cutscene | hide world-space UI for the duration of a cutscene | `flight-golden/` (the rings during the beat) |
| The kids on the biplane's bench sat with their hips on top of the box and overlapped into one shape | a standing pose lowered onto a seat | seated kids need a seated pose, not a lowered standing one | `_shared/rig.ts` seat pose (owed) |
| The Bog's frogs landed in open water and two of them sat inside one another, while a third never moved | the frogs sat on eight hand-typed coordinates while the lily pads were scattered by the flora pass, and a hop picked one of the three nearest "pads" with no occupancy test and no fallback | a creature that lives on props hops between the **rendered** props: the prop pass exports its instances, the creature pass seats and hops on those, occupancy is claimed the moment it leaves the ground (so a pad another is mid-air toward is taken), a seat is only offered where the graph gives it somewhere to go, and a stepped probe quotes the closest pair, the longest hop and the longest gap between moves | `bog-night/flora.ts` (`pads: PadSpot[]`), `bog-night/creatures.ts` (the hop graph and `owner`), `scripts/probes/bog-frogs.cjs` |
| A whirl fired in the cockpit: the ruby ribbon opened across the fuselage (a "long red line") and the kid spun through the plane | the flourish key is global and no scene could refuse it | an input that fires anywhere is vetoed by a scene that seats, stows or mounts the kids (`SceneWorld.flourishOk`); in the game a cutscene or a mount swallows ability inputs | `sandbox/_shared/scene.ts` (the X handler), `flight-golden/main.ts` |
| The goblin's arms read as "one short, one extra long, like some type of hammer but not in a good way", and the club swung through Isabella's head *(a Rigs row, filed here to keep this append at the file's end while other builders hold the same file)* | the club was a *limb extension* welded to a one-bone arm, so its far end went wherever the arm pointed, and the swing was one frame of `arm.rotation.x` | a creature that carries something gets the bible's own limb count (the Runt rig's `2 × arm × 3`: elbow and hand) and the thing it carries is **solved** between its contact point and the grip, with the hands IK'd onto it — never a shape bolted to a bone; the probe measures the prop's world position against the hero's `bones.head` through every frame of the swing | `meadow-golden/goblins.ts` (`ikArm`, `sweep`, the club on the root), `scripts/probes/meadow-goblins.cjs` |
| A two-bone IK arm spun 1.9 rad in a single frame mid-swing, and 0.8 rad mid-windup *(Rigs)* | (a) the elbow's pole is crossed somewhere in a 180° sweep, and at the crossing the roll about the arm's axis is undetermined; (b) a straight line between two grip poses on opposite sides of the shoulder passes within 0.04 m of it, and a fully folded arm has no direction either | carry the bend plane as a **tracked vector** and slew it (ease the number, then set the bone), and interpolate a hand's target **about the shoulder** — direction slerped, radius lerped — so the reach never leaves the arm's comfortable band; then a "nothing pops" probe (≤ 0.35 rad/frame, per state) can pass at all | `meadow-golden/goblins.ts` (`GobLimb.n`, `POLE_SLEW`, `sweep`), `scripts/probes/meadow-arm.cjs` |
| Three goblins arrived in a line, and giving each a slot 90° round the hero made one of them spiral 180° round it instead | a slot taken from the goblin's *current* bearing runs away from it as it flanks; and aiming straight at a 0.9 m slot from 15 m cuts a chord that crosses the attack trigger long before the slot is reached | a pack fans out by stamping each member's approach bearing **once**, when the chase opens, and approaching its slot's *radial* at a fraction of its current distance (a spiral); the slot ring stays strictly inside the attack trigger, or the member parks on its slot and never attacks — the never-attacks defect in a new hat | `meadow-golden/goblin-step.ts` (`slotPoint`, `SLOT_OFF`, `SLOT_CLOSE`), `meadow-golden/goblins.ts` |
| A landmark ringed round a play space read as a blank post from every station that was not the one it was carved for | a face carved on one side of a totem, and its facing formula turned round so even that side pointed away from the arena | a landmark inside a ring of stations is carved (or lit) on **every** side a station can stand on, and its facing is derived from the axis convention, not remembered — `xf`'s rotation maps local +z to (sin ry, cos ry), so "look at P" is `atan2(P.x − x, P.z − z)` | `meadow-golden/props.ts` (the four-sided war-totem head, `ry`) |
| Four kids rode the biplane with their hips inside the fuselage box and their legs coming out of its sides *(a Rigs row, filed at the file's end while other builders hold the same file)* | the rider's socket was a bench dropped into a **solid** body, and nothing ever tested a bone against the thing it was sitting in | a rider sits *in* or *on* real geometry: the body it rides is cut into parts with a real opening (nose, tail and an open well with a floor, walls, bulkheads and a rim), the socket is the **hip** point so every rider's hips land at one height whatever their leg length, and a stepped probe transforms hips, both knees, both feet, both hands and the head into the vehicle's own space and quotes the worst **signed** clearance against every solid as a box — the pose or the socket moves to pass, never the outline | `sandbox/_shared/plane.ts` (`WELL`, `solids`, `clearance`), `flight-golden/flight.ts` (`SEATS`, `gripFor`), `scripts/probes/flight-seats-04.cjs` |
| Ed's "red streamer" was a 2.2 m bar that stuck out ahead of the nose — Andrew: "the red line coming off of it" *(Misc)* | a piece of cloth modelled as a rigid box, welded to the top wing, with no chain and no wind | a streamer, scarf, banner or flag is a **chain of hinged quads** hung off a real fitting and trailing with the airflow, never a rod: each segment's *absolute* attitude is authored and the *difference from its parent* written on the bone (or the chain curls into a spiral at rest), the hang eases from vertical at wind 0 to nearly horizontal at full speed on two incommensurate rates, and a stepped probe proves the tip stays downstream of its root and hangs below it when the thing is parked | `sandbox/_shared/plane.ts` (`pennantAttitude`, the four quads on the rear outer strut), `scripts/probes/flight-pennant-04.cjs` |
| The caves' floor had to be flattened to one height to make the ring lie on it and the walk agree with it, and the family missed the chunky rock it lost *(a **Geometry** row, filed at the file's end while other builders hold the same file)* | relief was a *decorative* jitter on the drawn tops that `groundY` knew nothing about, so the only way to make the two agree was to delete it | **relief is walked from the drawn geometry, not sprinkled on top of it**: the surface's own primitives carry a quantised height, `groundY` returns the highest *drawn* primitive whose face covers the point (a point-in-primitive test, spatially hashed), the quantisation is relaxed so no two primitives a sample pair can span differ by more than the walk's step limit, and the anti-z-fight sink is *included in the returned height* rather than added after it — then the look and the walk are the same object and neither has to be given up | `caves-descent/terrain.ts` (`Column`, `columnTopAt`, `rawLevel` + the two relaxation passes), `tests/unit/sandbox/caves.test.ts` |
| A stepped surface was drawn from one parameterisation and walked from another, and every step boundary disagreed by a whole riser *(**Geometry**)* | the drawn steps were cut at round metres of arc while the walked height was cut at the resampled cross-sections' mitre lines, and the two are 0.15 m apart | a stair (or any stepped strip) is cut into steps **by cross-section index**, so the drawn seam and the walked seam are the same line; and a level tread takes the ramp's height at the step's *end*, which makes every riser equal and lands the foot exactly on the polyline's last y | `caves-descent/terrain.ts` (`STEP_SUB`, `p.stepArc`, `treadY`), `scripts/probes/caves-stair.cjs` (the largest single-frame Δy) |
| A selection ring lay flat on ground that stepped 0.54 m inside its own 1.6 m disc, so half of it hung in the air and half was buried *(a **Rigs** row, filed at the file's end)* | a ground decal is one quad at one height, and the only fix anyone had was to flatten the ground under it | **a ground decal conforms per vertex**: a float attribute per vertex, written from the scene's own `groundY` at that vertex's *current* world xz and added to world y **after** the curved-world term (never folded into it — the curve rides shared uniform objects by reference, T-41), refreshed only on frames the owner moved; the probe reads the attribute and raycasts the drawn rock under the xz the rig recorded, so it compares like with like. Watch the radial resolution: the bright rim is interpolated between radial rings, so a step that falls *between* two rings is ramped across, not followed | `sandbox/_shared/rig.ts` (the ring's `aLift`, `updateRingLift`), `scripts/probes/caves-ring-04.cjs` |
| Two cave salamanders hung in mid-air over the tiers and drifted slowly toward whoever stood still — Andrew: "are they supposed to float?" *(a **Density and scale** row, filed at the file's end)* | they were placed at two hand-typed world points on a stair wall, and the next round rebuilt the stair under them | **a clinging creature is placed by a cast against the surface it clings to, and lives in that surface's coordinates**: store it as (which path, how far along, how high above the tread), cast across the band every frame, put the belly on the hit and build the body's orientation as a **matrix basis** from the hit normal (local up) and the surface's tangent (local forward) rather than an Euler — nothing accumulates and nothing needs wrapping. The move along the surface is **marched**, not assumed, so the target never walks off the end of the wall; and the side it is on is kept unless that side runs out, or a nearer far wall flips the body through 180° mid-climb | `caves-descent/creatures.ts` (`cast`, `settle`, `reachable`, the side lock), `scripts/probes/caves-salamander-04.cjs` |
