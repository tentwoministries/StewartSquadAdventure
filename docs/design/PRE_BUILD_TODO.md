# Potential to-do list, before the master build (started 2026-09-08)

Things Andrew wants done or considered between the demo passes and the start of the build phases. Not a plan and not a promise: a list, each item with where it would run and what it needs. Items move to `docs/SESSION_PLAN.md` when scheduled and get a `DECISIONS.md` line when done or dropped.

| # | Item | Lane / session | Needs first | Status |
|---|---|---|---|---|
| P-01 | **Experience balance and upgrade brainstorm.** Before the master build, one session evaluates and balances the experiences across the game: a list of what the kids like (from the reel viewings and the story lane's `SUGGESTIONS.md`); each biome's distinctive thing (the caves' descent from the high rock into the deep is the model: one epic, novel traversal per zone); a deliberate spread of epic and novel moments across the islands so no zone is flat; and weighting toward what the kids like most, by putting more of it in more places or making it larger where it already is. Output: a balance table (zone × experience type × epic moment × kid-favourite weight) and a list of proposed upgrades as tweak or suggestion rows, for the application steps to carry into the bible. Andrew, 2026-09-08. | 0.85 story and play (after the walkthrough's kids' pass) | the reel viewings and the kids' suggestions to exist; the demo-candidates table | open |
| P-02 | **The Opus fixes** (`docs/design/mockups/OPUS_FIX_PLAN.md`). | 0.75 visual studies | nothing | open |
| P-03 | **The control build and its blind review** (`FABLE_CONTROL_BRIEF.md`); revise `CLAUDE.md` item 8 if it says so. | Fable control lane | nothing | open |
| P-04 | **Demo candidates** (`DEMO_PROGRAM.md` §2) and the next demo set. | 0.75 visual studies | P-02 | open |
| P-05 | **The caves' stairs and the rock** (observed by Andrew, not yet fixed; logged as tweak row T-34): the stairs visibly attach at the tiers' insertion points at the top of the descent, and the kids should not clip into the rock as they walk down. The first fixed example of a `misc`-tagged row. | 0.75 visual studies, with P-04 or a scratch pass | nothing | open |
| P-06 | **The anti-palette check scene** (`STUDY_NOTES.md` §3 row 6). | 0.75 visual studies | nothing | open |
| P-07 | **The 0.75 application step** (`PHASE_0.75_BRIEF.md` §4): rows applied, `p0.75-design-locked`, the ledger's `story-dependent` rows re-read. | 0.75 visual studies | P-02, P-04 and Andrew's pass on the reel | open |
| P-08 | **The 0.85 application step** (`docs/story/PHASE_0.85_BRIEF.md` §4): suggestions applied, `p0.85-story-locked`. | 0.85 story and play | the kids' reading sessions, P-01 | open |
| P-09 | **The full-ledger review before Phase 1** (`DEMO_PROGRAM.md` §1): qa-inspector reads every `LESSONS.md` row against the locked design once, promotes `misc` clusters into tags, and confirms every row that can be a check has one. | main, at the Phase 1 handoff | P-07, P-08 | open |
